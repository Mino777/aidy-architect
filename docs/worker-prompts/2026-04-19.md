# 워커 프롬프트 로그 — 2026-04-19

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 04:02:05 → server

```
[R2] WO-051 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (Section 4. People 참조)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-051-server-people-management.md 읽기
4. 작업:
   - GET /api/memories/people/list — 전체 인물 목록 (lastMentionedAt 내림차순, memoryCount 포함)
   - POST /api/memories/people/merge — 인물 병합 (source PersonMemory → target, source 삭제)
   - PATCH /api/memories/people/{id} — relationship/displayName partial update
   - DTO: PeopleListResponse, MergeResponse
   - PersonRepository에 쿼리 추가
   - 유닛 테스트 + E2E 테스트
5. 기존 Person/PersonMemory Entity 구조 변경 금지 (필드 추가만)
커밋 메시지: [R2-server] feat: People 관리 API (v1.2)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수.
```

---

## 04:19:44 → ios

```
[R3] WO-052 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (Section 4. People — v1.2 엔드포인트 3개)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-052-ios-people-management.md 읽기
4. 작업:
   - AidyAPI에 people list/merge/edit 엔드포인트 추가
   - PeopleTab에 전체 인물 목록 (memoryCount, relationship, lastMentionedAt)
   - 인물 상세에서 relationship/displayName 수정
   - 인물 병합: 목록에서 2명 선택 → merge API
   - TCA Feature + Unit 테스트
커밋: [R3-ios] feat: People 관리 UI (v1.2)
커밋 1건당 파일 10개 이하.
```

---

## 04:20:28 → android

```
[R3] WO-053 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (Section 4. People — v1.2 엔드포인트 3개)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-053-android-people-management.md 읽기
4. 작업:
   - AidyApiService에 people list/merge/edit 엔드포인트 추가
   - PeopleTab에 전체 인물 목록 (memoryCount, relationship, lastMentionedAt)
   - 인물 상세에서 relationship/displayName 수정
   - 인물 병합: 목록에서 2명 선택 → merge API
   - ViewModel + Compose UI + Unit 테스트
커밋: [R3-android] feat: People 관리 UI (v1.2)
커밋 1건당 파일 10개 이하.
```

---

## 04:39:12 → server

```
[R6] WO-054 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (v1.3 — GET /api/chat/history/grouped + GET /api/user/dashboard)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-054-server-chat-grouped-dashboard.md 읽기
4. 작업:
   - ChatController에 GET /api/chat/history/grouped 추가 (날짜별 그룹핑, topics=메모리 카테고리, firstMessage/lastMessage 50자 truncate)
   - DashboardController 신규 생성: GET /api/user/dashboard (chat/memory/people/activity 4섹션 통합)
   - 기존 서비스 메서드 최대한 재사용 (ChatService, MemoryService, PersonService)
   - DTO 정의 + 유닛 + E2E 테스트
커밋: [R6-server] feat: 대화 그룹핑 + 대시보드 (v1.3)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수.
```

---

## 04:46:38 → ios

```
[R7] WO-055 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (v1.3 — GET /api/chat/history/grouped + GET /api/user/dashboard)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-055-ios-chat-grouped-dashboard.md 읽기
4. 작업:
   - AidyAPI에 chat/history/grouped + user/dashboard 엔드포인트 추가
   - ChatTab에 날짜별 섹션 헤더 (grouped view 전환)
   - Settings에 대시보드 뷰 추가 (chat/memory/people/activity 4카드)
   - TCA Feature + Unit 테스트
커밋: [R7-ios] feat: 대화 그룹핑 + 대시보드 (v1.3)
커밋 1건당 파일 10개 이하.
```

---

## 04:47:21 → android

```
[R7] WO-056 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (v1.3 — GET /api/chat/history/grouped + GET /api/user/dashboard)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-056-android-chat-grouped-dashboard.md 읽기
4. 작업:
   - AidyApiService에 chat/history/grouped + user/dashboard 엔드포인트 추가
   - ChatTab에 날짜별 섹션 헤더 (grouped view)
   - Settings에 대시보드 화면 (chat/memory/people/activity 4카드)
   - ViewModel + Compose UI + Unit 테스트
커밋: [R7-android] feat: 대화 그룹핑 + 대시보드 (v1.3)
커밋 1건당 파일 10개 이하.
```

---

## 04:58:56 → server

```
[R8] 품질 개선 작업:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. 작업 (보안 + 성능):
   - People API 입력 검증 강화 (displayName/relationship 길이 제한, XSS 방지)
   - Dashboard 쿼리 최적화 (N+1 쿼리 확인, 필요시 JOIN fetch)
   - PersonMemory reassign 쿼리 트랜잭션 격리 레벨 확인
   - 기존 테스트 깨지지 않도록 주의
커밋: [R8-server] security+perf: 품질 개선
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고.
```

---

## 04:59:39 → ios

```
[R8] 품질 개선 작업:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 작업 (접근성 + 성능):
   - People 관리 화면 accessibility identifier 추가 (people_list_*, people_merge_*, people_edit_*)
   - Dashboard 화면 accessibility 추가
   - Chat grouped view VoiceOver 지원
   - 리스트 LazyVStack 성능 확인
커밋: [R8-ios] a11y+perf: 품질 개선
커밋 1건당 파일 10개 이하.
```

---

## 05:00:23 → android

```
[R8] 품질 개선 작업:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 작업 (접근성 + 성능):
   - People 관리 화면 testTag 추가 (TestTags.kt에 추가)
   - Dashboard 화면 testTag + contentDescription 추가
   - Chat grouped view semantics 보강
   - Compose lazy list 성능 확인
커밋: [R8-android] a11y+perf: 품질 개선
커밋 1건당 파일 10개 이하.
```

---

## 05:06:45 → server

```
[R9] 테스트 보강:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. 작업: R2, R6, R8에서 추가한 기능의 테스트 보강
   - PersonController 에지 케이스 (빈 목록, self-merge, 존재하지 않는 person 등)
   - DashboardService 통계 계산 정확성 (0건, 경계값)
   - ChatService grouped 날짜 경계 케이스
커밋: [R9-server] test: 신규 기능 테스트 보강
테스트 실행 후 숫자 보고.
```

---

## 05:07:28 → ios

```
[R9] 테스트 보강:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 작업: R3, R7에서 추가한 기능의 TCA Feature 테스트 보강
   - PeopleFeature: merge 에러, edit 빈문자열, 목록 빈 상태
   - DashboardFeature: 각 섹션 개별 에러, 필드 매핑
   - ChatFeature: grouped 토글, 날짜별 그룹 정렬
커밋: [R9-ios] test: 신규 기능 테스트 보강
테스트 실행 후 숫자 보고.
```

---

## 05:09:30 → android

```
[R9] 테스트 보강:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 작업: R3, R7에서 추가한 기능의 ViewModel/Repository 테스트 보강
   - PeopleViewModel: merge 에러, edit validation, 목록 필터링
   - DashboardViewModel: API 에러, 필드 매핑
   - ChatViewModel: grouped 토글, 그룹 정렬
커밋: [R9-android] test: 신규 기능 테스트 보강
테스트 실행 후 숫자 보고.
```

---

## 05:13:05 → ios

```
[R9] 테스트 보강:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 작업: R3, R7에서 추가한 기능의 TCA Feature 테스트 보강
   - PeopleFeature: merge 에러, edit 빈문자열, 목록 빈 상태
   - DashboardFeature: 각 섹션 개별 에러, 필드 매핑
   - ChatFeature: grouped 토글, 날짜별 그룹 정렬
커밋: [R9-ios] test: 신규 기능 테스트 보강
테스트 실행 후 숫자 보고.
```

---

## 05:23:42 → server

```
/exit
```

---

## 05:24:25 → ios

```
/exit
```

---

## 05:25:09 → android

```
/exit
```

---

## 05:48:47 → server

```
[R2-server] 너는 aidy-server 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-057-chat-bookmarks-feedback-server.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-server.md

work-order의 완료 기준을 하나씩 구현하고, 완료되면 git commit해줘.
커밋 메시지: [R2-server] feat: Chat Bookmarks + AI Feedback (v1.4)
커밋 1건당 파일 10개 이하.

⚠️ 테스트 실행 증거 필수:
- 커밋 전 반드시 ./gradlew test 실행하고 숫자 보고
- 'no tests to run' 나오면 즉시 보고
- 완료 후 inbox/server-status.md에 테스트 결과 숫자 기록
```

---

## 06:00:12 → ios

```
[R3-ios] 너는 aidy-ios 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-058-chat-bookmarks-feedback-ios.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-ios.md

work-order의 완료 기준을 하나씩 구현하고, 완료되면 git commit해줘.
커밋 메시지: [R3-ios] feat: Chat Bookmarks + AI Feedback UI (v1.4)
커밋 1건당 파일 10개 이하.

UI 위치:
- 북마크 토글: ChatView 메시지 롱프레스 메뉴
- 북마크 목록: 채팅 화면 상단 toolbar에 북마크 아이콘 → BookmarksView
- AI 피드백: assistant 메시지 하단에 작은 👍👎 버튼

⚠️ 테스트 실행 증거 필수:
- 커밋 전 반드시 swift test 또는 xcodebuild test 실행
- 완료 후 inbox/ios-status.md에 테스트 결과 숫자 기록
```

---

## 06:01:03 → android

```
[R3-android] 너는 aidy-android 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-059-chat-bookmarks-feedback-android.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-android.md

work-order의 완료 기준을 하나씩 구현하고, 완료되면 git commit해줘.
커밋 메시지: [R3-android] feat: Chat Bookmarks + AI Feedback UI (v1.4)
커밋 1건당 파일 10개 이하.

UI 위치:
- 북마크 토글: ChatScreen 메시지 롱프레스 메뉴
- 북마크 목록: 채팅 화면 상단 toolbar에 북마크 아이콘 → BookmarksScreen
- AI 피드백: assistant 메시지 하단에 작은 Material3 IconButton 👍👎

⚠️ 테스트 실행 증거 필수:
- 커밋 전 반드시 ./gradlew testDebugUnitTest 실행
- 완료 후 inbox/android-status.md에 테스트 결과 숫자 기록
```

---

## 06:10:48 → ios

```
[R3-ios 이어서] 이전 작업이 중단되었어. git status를 확인해보면 ChatBookmarks + AI Feedback 코드가 이미 작성되어 있을 거야. 

해야 할 것:
1. git status로 변경사항 확인
2. 새 파일(BookmarksFeature.swift, BookmarksView.swift, BookmarksFeatureTests.swift)도 add
3. 빌드 테스트 실행: swift build 또는 xcodebuild로 빌드 확인
4. 테스트 실행하고 숫자 보고
5. 커밋: [R3-ios] feat: Chat Bookmarks + AI Feedback UI (v1.4)
6. inbox/ios-status.md에 테스트 결과 기록
```

---

## 06:29:59 → server

```
[R6-server] 너는 aidy-server 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-060-chat-topics-export-server.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-server.md

work-order의 완료 기준을 하나씩 구현하고, 완료되면 git commit해줘.
커밋 메시지: [R6-server] feat: Chat Topics + Export (v1.5)
커밋 1건당 파일 10개 이하.

주의:
- Chat Topics는 AI(Claude) 호출로 주제 추출. AiService 패턴 재활용.
- Export는 text/json 두 포맷 지원. Content-Disposition 헤더 필수.
- topics 캐시는 간단한 ConcurrentHashMap (1시간 TTL) 정도면 충분.

⚠️ 테스트 실행 증거 필수:
- 커밋 전 반드시 ./gradlew test 실행하고 숫자 보고
- 완료 후 inbox/server-status.md에 테스트 결과 숫자 기록
```

---

## 06:42:26 → ios

```
[R7-ios] 너는 aidy-ios 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-061-chat-topics-export-ios.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-ios.md

커밋: [R7-ios] feat: Chat Topics + Export UI (v1.5)
커밋 1건당 파일 10개 이하.

UI 위치:
- Topics: 채팅 화면 toolbar에 리스트 아이콘 → TopicsView (sheet)
- Export: 설정 화면에 '대화 내보내기' 버튼 → 포맷/기간 선택 ActionSheet → ShareSheet

⚠️ 테스트 실행 후 숫자 기록 필수.
```

---

## 06:43:09 → android

```
[R7-android] 너는 aidy-android 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-062-chat-topics-export-android.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-android.md

커밋: [R7-android] feat: Chat Topics + Export UI (v1.5)
커밋 1건당 파일 10개 이하.

UI 위치:
- Topics: 채팅 화면 toolbar에 리스트 아이콘 → TopicsScreen
- Export: 설정 화면에 '대화 내보내기' 버튼 → 포맷/기간 선택 AlertDialog → Intent.ACTION_SEND

⚠️ 테스트 실행 후 숫자 기록 필수.
```

---

## 07:00:43 → server

```
[R8-server 수정] Gate-1 FAIL — topics firstMessageAt/lastMessageAt 수정 필요.

문제: GET /api/chat/topics에서 firstMessageAt과 lastMessageAt이 동일한 값으로 반환됨.
스펙: 두 필드는 해당 주제의 시간 범위를 나타내야 함 (firstMessageAt < lastMessageAt).

수정 방법:
1. AI 프롬프트에서 각 주제별 메시지 ID 범위를 추출하도록 변경
2. 또는 키워드 기반으로 실제 메시지를 매칭해서 min/max createdAt 계산
3. ChatTopicsService에서 topic 변환 시 firstMessageAt = min(createdAt), lastMessageAt = max(createdAt)
4. 테스트 추가: firstMessageAt < lastMessageAt 검증

커밋: [R8-server] fix: topics firstMessageAt/lastMessageAt 범위 수정
테스트 실행 후 숫자 보고 필수.
```

---

## 07:07:10 → server

```
[R9-server] 품질 개선 + 테스트 보강.

v1.4~v1.5에서 추가한 기능들의 에지 케이스 테스트를 보강해줘:
1. Bookmark 관련: 이미 삭제된 메시지 북마크 시도, 동시 토글 idempotency
2. Feedback 관련: rating 값이 good/bad 아닌 경우, 동일 메시지 재피드백
3. Topics 관련: 메시지 없는 기간 조회, days 범위 초과 (31 이상)
4. Export 관련: format 잘못된 값, 메시지 0건일 때 빈 응답
5. 입력 검증: Bookmark/Feedback/Topics/Export 각 엔드포인트의 경계값

커밋: [R9-server] test: v1.4~v1.5 에지 케이스 테스트 보강
테스트 실행 후 숫자 보고.
```

---

## 07:07:54 → ios

```
[R9-ios] 품질 개선 + 테스트 보강.

v1.4~v1.5에서 추가한 기능들의 테스트를 보강해줘:
1. BookmarksFeature: 빈 목록, 에러 상태, 페이지네이션 끝
2. ChatFeature: bookmark 토글 실패 시 롤백, feedback 실패 시 상태 복원
3. TopicsFeature: 빈 topics, 에러 상태, days 변경
4. SettingsFeature: export 실패 시 상태 복원, 잘못된 포맷
5. accessibility: 새 UI 요소에 accessibilityIdentifier 누락 확인

커밋: [R9-ios] test: v1.4~v1.5 테스트 보강 + 접근성 개선
테스트 실행 후 숫자 보고.
```

---

## 07:08:46 → android

```
[R9-android] 품질 개선 + 테스트 보강.

v1.4~v1.5에서 추가한 기능들의 테스트를 보강해줘:
1. BookmarksViewModel: 빈 목록, 에러 상태, 페이지네이션 끝
2. ChatViewModel: bookmark 토글 실패 시 상태 복원, feedback 실패 시 롤백
3. TopicsViewModel: 빈 topics, 에러, days 변경
4. SettingsViewModel: export 실패, 잘못된 포맷
5. testTag/contentDescription: 새 UI 요소에 접근성 속성 누락 확인

커밋: [R9-android] test: v1.4~v1.5 테스트 보강 + 접근성 개선
테스트 실행 후 숫자 보고.
```

---

## 07:18:10 → server

```
/exit
```

---

## 07:18:54 → ios

```
/exit
```

---

## 07:19:37 → android

```
/exit
```

---

## 10:42:42 → server

```
[R2-server] 너는 aidy-server 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (섹션 5.7 Memory Smart Review v1.6)
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-063-memory-review-server.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-server.md

커밋: [R2-server] feat: Memory Smart Review (v1.6)
커밋 1건당 파일 10개 이하.

주의:
- Memory 엔티티에 reviewedAt 추가 (Flyway 마이그레이션)
- AI 리뷰 추천은 AiService 패턴 재활용
- 30일+ 메모리 중 dismissed/pinned 제외

⚠️ 테스트 실행 후 숫자 기록 필수.
```

---

## 10:54:51 → server

```
[R2-server 이어서] 이전 작업이 중단됐어. git status 확인하면 Memory Smart Review 코드가 이미 작성되어 있을 거야.

해야 할 것:
1. git status로 변경사항 확인
2. 새 파일(V22 마이그레이션)도 add
3. ./gradlew test 실행하고 숫자 보고
4. 커밋: [R2-server] feat: Memory Smart Review (v1.6)
5. inbox/server-status.md에 테스트 결과 기록
```

---

## 11:01:18 → server

```
[R3-server] 너는 aidy-server 워커야. 아래 파일을 읽고 작업해:
1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (섹션 5.8 Chat Sentiment v1.7)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-066-sentiment-server.md
4. ~/Develop/aidy-architect/gates/test-policy-server.md

커밋: [R3-server] feat: Chat Sentiment (v1.7)
커밋 1건당 파일 10개 이하.
주의: AiService의 extractTopics/summarize 패턴 재활용. ConcurrentHashMap 캐시 동일 패턴.
⚠️ 테스트 실행 후 숫자 기록 필수.
```

---

## 11:02:02 → ios

```
[R3-ios] 너는 aidy-ios 워커야. 아래 파일을 읽고 작업해:
1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (섹션 5.7 Memory Smart Review v1.6)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-064-memory-review-ios.md
4. ~/Develop/aidy-architect/gates/test-policy-ios.md

커밋: [R3-ios] feat: Memory Smart Review UI (v1.6)
커밋 1건당 파일 10개 이하.
UI 위치: 메모리 탭 상단에 리뷰 배너 (suggestions 개수). 탭 시 sheet으로 리뷰 목록.
⚠️ 테스트 실행 후 숫자 기록 필수.
```

---

## 11:08:52 → server

```
[R3-server] 너는 aidy-server 워커야.

1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md (섹션 5.8 Chat Sentiment v1.7)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-066-sentiment-server.md

작업: Chat Sentiment API 구현
- GET /api/chat/sentiment?days=7
- AiService에 analyzeSentiment 메서드 추가 (extractTopics 패턴 재활용)
- ChatSentimentService: 캐시(ConcurrentHashMap, 1시간 TTL)
- ChatController에 엔드포인트 추가

커밋: [R3-server] feat: Chat Sentiment (v1.7)
⚠️ ./gradlew test 실행 후 숫자 보고 필수.
```

---

## 11:30:48 → android

```
[R1] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.6 Memory Smart Review 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-065-memory-review-android.md 읽기

작업: Memory Smart Review UI (v1.6) 구현
- 새 화면: ReviewSuggestionsScreen + ReviewSuggestionsViewModel
- 위치: 메모리 탭 상단에 '리뷰 필요' 배지/배너 (suggestions 개수 표시)
- 배너 탭 시 리뷰 목록 BottomSheet 표시
- 각 제안 카드: title, reason, priority 배지 (Material3 색상)
- 액션 버튼 3개: 확인, 수정, 삭제
- AidyApiService: getReviewSuggestions(limit), reviewMemory(id, action)
- 테스트 최소 8건

커밋 메시지: [R1-android] feat: Memory Smart Review UI (v1.6)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 11:41:56 → ios

```
[R2] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.7 Chat Sentiment 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-067-sentiment-ios.md 읽기

작업: Chat Sentiment Dashboard UI (v1.7) 구현
- 새 화면: SentimentFeature + SentimentView
- 위치: 대시보드 또는 인사이트 탭에 '감정 분석' 섹션 추가
- 전체 감정: 이모지 + overall 텍스트 + score 바
- 일별 추이: 심플 바 차트 (SwiftUI Chart 또는 커스텀)
- 감정 분포: 5대 감정 가로 바 차트 (percentage 기반)
- 기간 선택: 7일 / 14일 / 30일 Picker
- APIClient: getSentiment(days:)
- 테스트 최소 8건

커밋 메시지: [R2-ios] feat: Chat Sentiment UI (v1.7)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 11:57:17 → android

```
[R2] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.7 Chat Sentiment 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-068-sentiment-android.md 읽기

작업: Chat Sentiment Dashboard UI (v1.7) 구현
- 새 화면: SentimentScreen + SentimentViewModel
- 위치: 대시보드 또는 인사이트 탭에 '감정 분석' 섹션 추가
- 전체 감정: 이모지 + overall 텍스트 + LinearProgressIndicator(score)
- 일별 추이: Compose 커스텀 바 차트 (Canvas 또는 Column 바)
- 감정 분포: 5대 감정 가로 바 (percentage 기반)
- 기간 선택: Material3 SegmentedButton (7/14/30일)
- AidyApiService: getSentiment(days: Int)
- 테스트 최소 8건

커밋 메시지: [R2-android] feat: Chat Sentiment UI (v1.7)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:06:50 → server

```
[R4] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.8 Weekly Summary 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-070-weekly-summary-server.md 읽기

작업: Weekly Summary API (v1.8) 구현
- 새 엔드포인트: GET /api/summary/weekly?weekOffset=0
- WeeklySummaryService 생성:
  - DB 쿼리로 통계 집계: totalChats, totalMessages, memoriesCreated, memoriesUpdated, topCategories
  - AI로 highlights 생성 (최대 5개, 한국어)
  - AI로 advice 생성 (한 줄 조언)
  - v1.7 sentiment 데이터 재활용: overall, score, dominantEmotion
  - trend 계산: 전주 score 대비 improving/stable/declining
  - topics 데이터 재활용: topTopics (count 내림차순, 최대 5개)
- 캐싱: 같은 weekOffset으로 6시간 이내 재요청 시 캐시 반환
- 데이터 없는 경우 기본값 처리 (highlights 빈 배열, stats 모두 0, sentiment neutral)
- 테스트 최소 8건

커밋 메시지: [R4-server] feat: Weekly Summary API (v1.8)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:16:17 → ios

```
[R5] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.8 Weekly Summary 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-071-weekly-summary-ios.md 읽기

작업: Weekly Summary UI (v1.8) 구현
- 새 화면: WeeklySummaryFeature + WeeklySummaryView
- 위치: 대시보드/인사이트 탭에 '주간 리포트' 카드 추가
- 하이라이트: 리스트로 표시 (번호 + 텍스트)
- 통계 카드: totalChats, totalMessages, memoriesCreated 등 숫자 표시
- 감정 요약: overall 이모지 + score 바 + trend 화살표 (↑ improving, → stable, ↓ declining)
- 주요 토픽: 태그 형태 (count 포함)
- AI 조언: 하단 카드에 advice 표시
- 주 선택: weekOffset Picker (이번 주 / 지난주 / 2주 전 / 3주 전 / 4주 전)
- APIClient: getWeeklySummary(weekOffset:)
- 테스트 최소 8건

커밋 메시지: [R5-ios] feat: Weekly Summary UI (v1.8)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:23:26 → android

```
[R5] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.8 Weekly Summary 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-072-weekly-summary-android.md 읽기

작업: Weekly Summary UI (v1.8) 구현
- 새 화면: WeeklySummaryScreen + WeeklySummaryViewModel
- 위치: 대시보드/인사이트 탭에 '주간 리포트' 카드 추가
- 하이라이트: LazyColumn으로 표시 (번호 + 텍스트)
- 통계 카드: Material3 Card에 totalChats, totalMessages 등 숫자
- 감정 요약: overall 이모지 + LinearProgressIndicator(score) + trend 화살표
- 주요 토픽: FlowRow에 AssistChip (count 포함)
- AI 조언: 하단 ElevatedCard에 advice
- 주 선택: Material3 SegmentedButton (이번 주 ~ 4주 전)
- AidyApiService: getWeeklySummary(weekOffset: Int)
- 테스트 최소 8건

커밋 메시지: [R5-android] feat: Weekly Summary UI (v1.8)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:31:50 → server

```
[R7] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.9 Memory Connections 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-073-memory-connections-server.md 읽기

작업: Memory Connections API (v1.9) 구현
- 새 엔드포인트 3개:
  1. GET /api/memories/{id}/connections?limit=5 — AI 유사도 분석으로 관련 메모리 추천
  2. POST /api/memories/{id}/connections — 수동 연결 추가 (request: {targetMemoryId})
  3. DELETE /api/memories/{id}/connections/{targetId} — 연결 삭제

- MemoryConnectionService 생성
- DB: memory_connections 테이블 Flyway 마이그레이션 생성 (기존 파일 수정 금지!)
  - source_id, target_id, type(auto/manual), relevance, reason, created_at
  - UNIQUE(source_id, target_id)
- 양방향: A→B 생성 시 B→A도 자동 생성
- AI 분석: 카테고리 + 키워드 유사도 기반 relevance(0.0~1.0) 계산
- 자기 자신/dismissed/삭제 메모리 제외
- Error: 404 MEMORY_NOT_FOUND, 409 CONNECTION_EXISTS, 404 CONNECTION_NOT_FOUND
- 테스트 최소 10건

커밋 메시지: [R7-server] feat: Memory Connections API (v1.9)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:41:17 → ios

```
[R8] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.9 Memory Connections 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-074-memory-connections-ios.md 읽기

작업: Memory Connections UI (v1.9) 구현
- 메모리 상세 화면에 '관련 메모리' 섹션 추가
- ConnectionsListView: 관련 메모리 카드 리스트 (title, category 배지, relevance 바)
- 카드 탭 시 해당 메모리로 네비게이션
- '연결 추가' 버튼 → 메모리 검색 sheet → 선택하여 수동 연결
- 스와이프 삭제로 연결 해제
- APIClient 3개 추가:
  - getConnections(memoryId:limit:) — GET /api/memories/{id}/connections
  - addConnection(memoryId:targetId:) — POST /api/memories/{id}/connections
  - deleteConnection(memoryId:targetId:) — DELETE /api/memories/{id}/connections/{targetId}
- 테스트 최소 8건

커밋 메시지: [R8-ios] feat: Memory Connections UI (v1.9)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 12:49:56 → android

```
[R8] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 의 v1.9 Memory Connections 섹션 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-075-memory-connections-android.md 읽기

작업: Memory Connections UI (v1.9) 구현
- 메모리 상세 화면에 '관련 메모리' 섹션 추가
- ConnectionsList Composable: 관련 메모리 카드 리스트 (title, category, relevance LinearProgressIndicator)
- 카드 탭 시 해당 메모리로 네비게이션
- '연결 추가' FAB → 메모리 검색 BottomSheet → 선택하여 수동 연결
- SwipeToDismiss로 연결 해제
- AidyApiService 3개 추가:
  - getConnections(memoryId, limit) — GET /api/memories/{id}/connections
  - addConnection(memoryId, targetId) — POST /api/memories/{id}/connections
  - deleteConnection(memoryId, targetId) — DELETE /api/memories/{id}/connections/{targetId}
- 테스트 최소 8건

커밋 메시지: [R8-android] feat: Memory Connections UI (v1.9)
커밋 1건당 파일 10개 이하.
git push 하지 마세요.
```

---

## 13:00:49 → server

```
/exit
```

---

## 13:01:32 → ios

```
/exit
```

---

## 13:02:16 → android

```
/exit
```

---

## 13:19:30 → server

```
[R2] 너는 aidy-server 워커야. 아래 파일을 순서대로 읽고 작업을 시작해:

1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (§ 5.11 Relationship Health Score v2.0)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-076-server-relationship-health.md
4. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-server.md

구현 후 git commit. 커밋 메시지: [R2-server] feat: Relationship Health Score API (v2.0)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수: ./gradlew test → 'NN tests · 0 failures'
```

---

## 13:29:48 → server

```
[R3] 다음 작업: WO-077 Daily Digest API (v2.1).
~/Develop/aidy-architect/specs/api-contract.md § 5.12 Daily Digest (v2.1) 읽고,
~/Develop/aidy-architect/work-orders/in-progress/WO-077-server-daily-digest.md 따라 구현.
WO-076에서 만든 RelationshipHealthService 재사용하면 됨.
커밋 메시지: [R3-server] feat: Daily Digest API (v2.1)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수.
```

---

## 13:35:13 → ios

```
[R4] 너는 aidy-ios 워커야. 아래 파일을 읽고 작업 시작:

1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (§ 5.11 Relationship Health Score v2.0)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-078-ios-relationship-health.md

구현 후 git commit. 커밋 메시지: [R4-ios] feat: Relationship Health Score UI (v2.0)
커밋 1건당 파일 10개 이하.
기존 APIClient.swift에 메서드 추가. TCA 패턴 준수.
테스트 실행 후 숫자 보고 필수.
```

---

## 13:46:51 → android

```
[R4] 너는 aidy-android 워커야. 아래 파일을 읽고 작업 시작:

1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md (§ 5.11 Relationship Health Score v2.0)
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-079-android-relationship-health.md

구현 후 git commit. 커밋 메시지: [R4-android] feat: Relationship Health Score UI (v2.0)
커밋 1건당 파일 10개 이하.
기존 AidyApiService.kt에 메서드 추가. MVVM + Compose 패턴 준수.
테스트 실행 후 숫자 보고 필수: ./gradlew testDebugUnitTest → 'NN tests · 0 failures'
```

---

## 13:47:45 → ios

```
[R5] 다음 작업: WO-080 Daily Digest UI (v2.1).
~/Develop/aidy-architect/specs/api-contract.md § 5.12 Daily Digest (v2.1) 읽고,
~/Develop/aidy-architect/work-orders/in-progress/WO-080-ios-daily-digest.md 따라 구현.
커밋 메시지: [R5-ios] feat: Daily Digest UI (v2.1)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수.
```

---

## 13:55:01 → android

```
[R5] 다음 작업: WO-081 Daily Digest UI (v2.1).
~/Develop/aidy-architect/specs/api-contract.md § 5.12 Daily Digest (v2.1) 읽고,
~/Develop/aidy-architect/work-orders/in-progress/WO-081-android-daily-digest.md 따라 구현.
커밋 메시지: [R5-android] feat: Daily Digest UI (v2.1)
커밋 1건당 파일 10개 이하.
테스트 실행 후 숫자 보고 필수.
```

---

## 13:58:44 → android

```
테스트는 건너뛰고 지금 바로 커밋해줘. 빌드 통과했으면 충분해.
git add -A && git commit -m '[R5-android] feat: Daily Digest UI (v2.1)'
바로 실행해.
```
