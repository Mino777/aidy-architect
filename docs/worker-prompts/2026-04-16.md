# 워커 프롬프트 로그 — 2026-04-16

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 22:15:15 → server

```
[R9-server-s6] 작업 지시: GET /api/internal/stats/summary — 본인 요약 통계

1. test-policy-server.md 재확인
2. 신규 엔드포인트 GET /api/internal/stats/summary
   - 인증 필요 (JWT Bearer). principal 의 userId 본인 데이터만
   - Response: { aiCalls24h: int, aiSuccessRate: float (0.0~1.0), aiAvgDurationMs: int, errorLogs24h: int, memoriesTotal: int, peopleTotal: int }
3. 기존 Repository 재사용 (새 쿼리만 추가):
   - AiCallLogRepository: countByUserIdAndCreatedAtAfter, avgDurationMs, successCount (24h 기준)
   - ErrorLogRepository: countByUserIdAndCreatedAtAfter (24h)
   - MemoryRepository: countByUserId
   - PersonRepository: countByUserId
4. StatsService 또는 InternalController 내부 집계 로직
5. 컨트롤러 테스트 3건 이상: happy / 인증 없음 401 / 본인 외 데이터 노출 안 됨 (동시 여러 유저)
6. 실행 증거 필수: ./gradlew test → 207+ tests

금지: api-contract.md 변경 (internal 엔드포인트는 스펙에서 제외됨). 다른 유저 데이터 노출.
커밋: [R9-server] feat: GET /api/internal/stats/summary (파일 6개 이하)
커밋 메시지에 '테스트: NN passed, 0 failed' 포함 필수.
```

---

## 22:20:48 → ios

```
[R9-ios-s6] 작업 지시: 디버그 뷰 — 통계/상태 요약

1. test-policy-ios.md 재확인
2. APIClient.fetchStatsSummary() async throws -> StatsSummary 추가
   - GET /api/internal/stats/summary (Bearer 토큰)
   - StatsSummary: aiCalls24h, aiSuccessRate, aiAvgDurationMs, errorLogs24h, memoriesTotal, peopleTotal
3. SettingsView 에 '디버그 정보' 섹션 추가 (또는 기존 섹션 정리):
   - baseURL, 앱 버전 (기존)
   - DraftQueue count, ErrorLog 최근 5건 (기존 통합)
   - RequestMetrics (기존)
   - **서버 통계 요약** (새로 호출, 로딩/실패 '—' 표시)
4. SettingsFeature: loadStatsSummary 액션, state.statsSummary (optional)
5. TestStore 테스트 3건: 로드 성공 / 실패 시 nil 유지 / onAppear 자동 호출
6. 실행 증거 필수: xcodebuild test -workspace Aidy.xcworkspace -scheme Aidy -destination 'platform=iOS Simulator,name=iPhone 17,OS=latest' → 124+ tests

금지: 새 화면. 새 3rd party.
커밋: [R9-ios] feat: 디버그 뷰 + 서버 통계 연동 (파일 5개 이하)
커밋 메시지에 '테스트: NN passed' 포함 필수.
```

---

## 22:20:48 → android

```
[R9-android-s6] 작업 지시: 디버그 뷰 — 통계/상태 요약

1. test-policy-android.md 재확인
2. AidyApiService.getStatsSummary() suspend fun → StatsSummaryResponse 추가 (/api/internal/stats/summary, Authorization header)
3. SettingsRepository 또는 VM에서 직접 호출
4. SettingsScreen 에 '디버그 정보' 섹션:
   - baseUrl, 앱 버전 (기존)
   - DraftQueue count, ErrorLog 5건 (기존 통합)
   - RequestMetrics (기존)
   - **서버 통계 요약** — 로딩 중 스피너, 실패 시 '—'
5. SettingsViewModel: loadStatsSummary() + statsSummary StateFlow
6. ViewModel 테스트 3건: 성공 / 실패 시 null 유지 / 로딩 중 상태
7. 실행 증거 필수: ./gradlew testDebugUnitTest → 135+ tests

금지: 새 Hilt 모듈. 새 dependency.
커밋: [R9-android] feat: 디버그 뷰 + 서버 통계 연동 (파일 5개 이하)
커밋 메시지에 '테스트: NN passed' 포함 필수.
```

---

## 22:35:32 → ios

```
429로 중단된 [R9-ios-s6] 작업 이어서 완료해줘. 파일 저장된 부분부터 계속 — StatsSummary 타입 완성, APIClient.fetchStatsSummary, SettingsFeature loadStatsSummary 액션, SettingsView 섹션, TestStore 3건. 커밋 + 테스트 숫자 포함.
```

---

## 22:40:45 → android

```
429로 중단된 [R9-android-s6] 작업 이어서 완료해줘. 저장된 부분부터 계속 — AidyApiService.getStatsSummary, StatsSummaryResponse 모델, SettingsViewModel loadStatsSummary + StateFlow, SettingsScreen 디버그 섹션, ViewModel 테스트 3건. 커밋 + 테스트 숫자 포함.
```
