# 워커 프롬프트 로그 — 2026-04-25

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 00:24:33 → ios

```
[TMA 마이그레이션] Phase 2: Feature 모듈 TMA 분리 (전체 14개)

## 배경
Core TMA 분리 완료 (3b0d015). 이제 Feature 계층을 TMA 구조로 분리한다.

## 컨텍스트
1. ~/Develop/aidy-ios/Tuist/ProjectDescriptionHelpers/Module.swift — 헬퍼
2. ~/Develop/aidy-ios/Project.swift — 현재 구조 (Core 모듈 이미 분리됨)
3. 현재 Feature 위치: Projects/App/Sources/Feature/{14개 폴더}

## 작업

### 14개 Feature 모듈을 TMA 5-타겟으로 분리:
Auth, Chat, Memory, People, Search, Settings, Anniversary, Digest, HomeDashboard, InteractionLog, Nudge, Onboarding, RelationshipMap, SmartNotification

각 피처마다:
1. Projects/Feature/{Name}/Sources/ ← 구현 코드 이동
2. Projects/Feature/{Name}/Interface/ ← 공개 인터페이스 (Feature, Client protocol, Model)
3. Projects/Feature/{Name}/Tests/ ← 기존 테스트 이동
4. Projects/Feature/{Name}/Testing/ ← Mock/Stub 생성

### 의존성 규칙 (핵심)
- Feature → Feature: Interface만 의존 (.interface() 사용)
- Feature → Core: .module() 직접 의존 OK
- 모든 Feature → .tca 의존

### Project.swift 수정
Module 헬퍼로 Feature 모듈 정의. 메인 App은 모든 Feature 모듈에 의존.

### public 접근제어자
모듈 외부에서 참조하는 모든 타입/함수에 public 추가.
- Feature struct, State, Action, body → public
- View struct → public
- Client protocol/struct → public (Interface에 배치)

### 완료 기준
- [ ] 14개 Feature 모듈 TMA 구조로 재배치
- [ ] Project.swift에 Feature 모듈 정의
- [ ] tuist generate --no-open 성공
- [ ] xcodebuild build-for-testing 성공

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [TMA] type: 설명 (한글)
- 빌드 에러 → import 수정 + public 추가에 집중

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[ios 커밋] 해시 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 01:18:57 → ios

```
[TMA] SmartNotification 빌드 에러 수정

## 문제
SmartNotificationFeature.swift:48 — 'does not conform to protocol Reducer'
3개 에러, 모두 SmartNotification 모듈.

## 원인 추정
@Reducer 매크로 + @CasePathable 조합에서 모듈 간 타입 visibility 이슈.
다른 Feature 모듈에서 동일 패턴이 동작하므로, SmartNotification만의 특이사항 확인 필요.

## 작업
1. SmartNotificationFeature.swift 확인 — 다른 Feature와 비교
2. 에러 원인 찾아서 수정
3. xcodebuild build-for-testing 전체 빌드 통과 확인
4. 커밋

## 보고
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 21:03:16 → server

```
[R2] v5.0 서버 4개 피처 구현 지시:

## 필수 선행
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md §5.40~5.43 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-167~170 읽기

## 작업 (4개 피처, 순서대로)
1. WO-167: Mood Tracking — POST/DELETE /api/chat/{chatId}/mood + GET /api/mood/trends
2. WO-168: Frequency Goals — PUT/DELETE /api/people/{personId}/frequency-goal + GET /api/frequency-goals
3. WO-169: Communication Quality — GET /api/insights/communication-quality (4차원 점수 + AI suggestions)
4. WO-170: Milestones — GET/POST/PATCH/DELETE milestones + 자동 감지

## 커밋 규칙
- 커밋 메시지: [R2-server] type: 설명
- 커밋 1건당 파일 10개 이하
- 피처 2개씩 묶어 커밋 OK

## 제약
- 기존 Entity 구조 변경 금지 (새 Entity만 추가)
- 새 패키지 설치 금지
- DB 마이그레이션 파일 생성만 (실행 금지)

## 완료 보고
작업 완료 후: tmux send-keys -t aidy:0.0 '[server 완료] 커밋해시 커밋메시지' Enter
```

---

## 21:04:10 → ios

```
[R2] iOS 테스트 커버리지 보강 (WO-175):

## 필수 선행
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/work-orders/in-progress/WO-175-ios-test-parity.md 읽기

## 작업
1. 현재 테스트 실행하여 커버 안 된 Feature 식별
2. TMA Testing 타겟에 mock/stub 추가 (Core/Network, Feature/Chat, Feature/Memory, Feature/People)
3. 테스트 갭 해소: Chat, Memory, People, Settings Feature의 주요 Reducer 액션 + Effect 테스트
4. 목표: 748 → 900+ tests

## 커밋 규칙
- 커밋 메시지: [R2-ios] test: 설명
- 커밋 1건당 파일 10개 이하

## 제약
- 기능 코드 수정 금지 (테스트 코드만)
- 새 패키지 설치 금지

## 완료 보고
작업 완료 후: tmux send-keys -t aidy:0.0 '[ios 완료] 커밋해시 커밋메시지' Enter
```

---

## 22:05:28 → ios

```
xcodebuild test 중단하세요. 시뮬레이터 테스트는 너무 느립니다.

대신 이렇게 진행:
1. tuist test로 테스트 실행 (xcodebuild 대신)
2. tuist test가 안 되면 tuist build로 컴파일만 확인
3. 테스트 파일을 직접 작성하고 tuist build로 컴파일 통과만 확인
4. 테스트 로직은 TCA의 TestStore 기반으로 작성

xcodebuild test는 Gate-2에서 Architect가 직접 실행합니다.
지금은 테스트 코드 작성 + 빌드 통과에 집중하세요.
```

---

## 22:06:33 → android

```
[R3] Android v5.0 피처 구현 (WO-173, 174):

## 필수 선행
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md §5.40~5.43 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-173-android-mood-frequency.md 읽기
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-174-android-quality-milestones.md 읽기

## 작업 (4개 피처)
1. Mood Tracking: MoodRepository + MoodPickerRow (8이모지 Chip) + MoodTrendsScreen
2. Frequency Goals: FrequencyGoalRepository + FrequencyGoalScreen + PersonDetail에 설정 다이얼로그
3. Communication Quality: CommunicationQualityRepository + CommunicationQualityScreen (4차원 바 차트)
4. Milestones: MilestoneRepository + MilestoneScreen + PersonDetail에 이정표 섹션

## 커밋 규칙
- 커밋 메시지: [R3-android] type: 설명
- 커밋 1건당 파일 10개 이하
- 피처 2개씩 묶어 커밋 OK

## 제약
- 새 패키지 설치 금지 (기존 Compose/Hilt/Retrofit만)
- 기존 코드 구조 따르기 (data/domain/ui 레이어)

## 완료 보고
작업 완료 후: tmux send-keys -t aidy:0.0 '[android 완료] 커밋해시 커밋메시지' Enter
```

---

## 23:00:25 → ios

```
[R4] iOS v5.0 피처 구현 (WO-171, 172):

## 필수 선행
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md §5.40~5.43 읽기
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-171-ios-mood-frequency.md 읽기
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-172-ios-quality-milestones.md 읽기

## 작업 (4개 피처)
1. Mood Tracking: MoodClient + MoodPickerView (8개 이모지) + MoodTrendsFeature/View
2. Frequency Goals: FrequencyGoalClient + FrequencyGoalFeature + PersonDetail에 설정 시트
3. Communication Quality: CommunicationQualityClient + CommunicationQualityFeature/View
4. Milestones: MilestoneClient + MilestoneFeature/View + PersonDetail에 이정표 섹션

## 커밋 규칙
- 커밋 메시지: [R4-ios] feat: 설명
- 커밋 1건당 파일 10개 이하
- 피처 2개씩 묶어 커밋 OK
- xcodebuild test 하지 말 것. tuist build로 컴파일만 확인.

## 제약
- 새 패키지 설치 금지
- TMA 구조 준수 (Feature 모듈에 추가)

## 완료 보고
작업 완료 후: tmux send-keys -t aidy:0.0 '[ios 완료] 커밋해시 커밋메시지' Enter
```
