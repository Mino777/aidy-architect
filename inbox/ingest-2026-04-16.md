# /ingest 결과 — 워커 공유 알림

**일시**: 2026-04-16
**소스**: ai-study wiki (83 entries)

---

## 서버 워커 (aidy-server)

### 다음 WO 후보
1. **Circuit Breaker + Multi-Provider Fallback** (P-004)
   - AI 호출에 timeout + retry + 에러 분류 적용
   - 참고: ai-study Journal 006 패턴
2. **AI 출력 런타임 검증** (P-005)
   - Layer 1: 텍스트 가드 (길이, 금지 용어)
   - Layer 2: 스키마 검증 (type guard 또는 Zod)
   - Layer 3: retry with validation
   - Layer 4: instruction augmentation (실패 이유를 프롬프트에 추가)
   - Layer 5: LLM-as-Judge (품질 채점)
   - 참고: ai-study Journal 009-023

---

## iOS 워커 (aidy-ios)

### 확인 필요
- `.claudeignore`에 `**/.build/` 글로브 패턴 포함되어 있는지 확인
  - `.build/`만으로는 SPM 멀티모듈 하위 디렉터리 못 잡음
  - `vendor/`, `external-spm/`, `*.xcodeproj/`도 함께 차단 권장
  - 참고: ai-study iOS Harness Journal 003 (130GB 방지)

---

## Android 워커 (aidy-android)

### 확인 필요
- `.claudeignore`에 빌드 캐시 패턴 확인
  - `build/`, `.gradle/`, `.kotlin/` 제외 여부

---

## 전체 워커 공통

### CLAUDE.md 토큰 최적화 규칙 (신규)
- 활성 세션 중 CLAUDE.md 수정 금지 (캐시 무효화)
- 5분 TTL 리듬 유지
- `/compact` 습관화

### Compound 12 원칙
- `docs/compound-principles.md` 신규 생성됨
- WO 완료 후 /compound 실행 전 참고
