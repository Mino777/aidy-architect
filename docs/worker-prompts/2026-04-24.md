# 워커 프롬프트 로그 — 2026-04-24

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 18:20:09 → server

```
[R2] 작업 지시 (WO-158, WO-159, WO-160)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §5.37, §5.38, §5.39
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-158-server-avatar.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-159-server-search-filters.md
5. ~/Develop/aidy-architect/work-orders/in-progress/WO-160-server-heatmap.md

## 작업 (3개 피처)

### WO-158: User Avatar API (§5.37)
- UserAvatar 엔티티 + avatarUrl을 User에 추가
- POST/GET/DELETE /api/auth/avatar + GET /api/auth/avatar/{filename}
- login/signup/refresh 응답에 avatarUrl 추가
- 파일 저장: uploads/avatars/, javax.imageio로 200x200 썸네일
- AvatarControllerTest

### WO-159: Advanced Search Filters (§5.38)
- GET /api/search에 from/to/person/category/type/sort/limit/offset 추가
- Browse 모드: q없이 필터만으로 검색
- 기존 q-only 하위 호환 유지
- SearchControllerTest 보강

### WO-160: Activity Heatmap (§5.39)
- GET /api/activity/heatmap?year=2026&month=4
- 날짜별 chatCount/memoryCount 집계 (GROUP BY)
- level 0~4 사분위수 계산
- streak 계산 (current + longest)
- ActivityHeatmapControllerTest

### 완료 기준
- [ ] 3개 피처 구현
- [ ] 빌드 PASS: ./gradlew test
- [ ] 테스트 숫자 보고 (before: 1033)

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R2-server] feat/test: 설명 (한글), 파일 10개 이하
- WO별 1커밋 권장 (최대 2)

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[server 커밋] WO-XXX 해시 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[server 완료]' Enter
```

---

## 18:34:25 → ios

```
[R3] 작업 지시 (WO-161, WO-162, WO-163)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §5.37, §5.38, §5.39
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-161-ios-avatar.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-162-ios-search-filters.md
5. ~/Develop/aidy-architect/work-orders/in-progress/WO-163-ios-heatmap.md

## 작업 (3개 피처)

### WO-161: User Avatar UI (§5.37)
- AvatarClient (TCA Dependency): upload(multipart)/get/delete
- 프로필 화면: 원형 아바타 120px, PhotosPicker로 선택, 삭제 옵션
- 아바타 없으면 이니셜 (기존 패턴)
- AuthFeature state에 avatarUrl 추가 (login 응답에서)
- AvatarFeatureTests

### WO-162: Advanced Search Filters UI (§5.38)
- SearchClient 확장: from/to/person/category/type/sort/limit/offset
- 검색바 아래 필터 칩 바 (수평 스크롤): 날짜/인물/카테고리/타입/정렬
- 활성 필터 칩 X 해제, 페이지네이션
- SearchFilterFeatureTests

### WO-163: Activity Heatmap UI (§5.39)
- ActivityClient: getHeatmap(year, month)
- 7열×N행 그리드, level 색상 4단계 (Primary opacity 0.1/0.3/0.6/1.0)
- 월 네비게이션 (좌우 화살표)
- summary: activeDays, currentStreak, longestStreak
- ActivityHeatmapFeatureTests

### 완료 기준
- [ ] 3개 피처 UI + API Client
- [ ] 빌드 PASS: xcodebuild build-for-testing
- [ ] 테스트 숫자 보고 (before → after)

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R3-ios] feat: 설명 (한글), 파일 10개 이하
- WO별 1커밋 권장

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[ios 커밋] WO-XXX 해시 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 18:35:19 → android

```
[R3] 작업 지시 (WO-164, WO-165, WO-166)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §5.37, §5.38, §5.39
3. ~/Develop/aidy-architect/work-orders/in-progress/WO-164-android-avatar.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-165-android-search-filters.md
5. ~/Develop/aidy-architect/work-orders/in-progress/WO-166-android-heatmap.md

## 작업 (3개 피처)

### WO-164: User Avatar UI (§5.37)
- AvatarRepository + AvatarApi: upload(multipart)/get/delete
- 프로필: 원형 아바타 120dp (Coil AsyncImage), PickVisualMedia로 선택
- 아바타 없으면 이니셜
- 로그인 응답 avatarUrl 활용
- AvatarViewModelTest

### WO-165: Advanced Search Filters UI (§5.38)
- SearchRepository 확장: from/to/person/category/type/sort/limit/offset
- LazyRow 필터 칩 바: 날짜(DateRangePicker)/인물/카테고리/타입/정렬
- FilterChip X 해제, LazyColumn 페이지네이션
- SearchViewModelTest 보강

### WO-166: Activity Heatmap UI (§5.39)
- ActivityRepository + ActivityApi: getHeatmap(year, month)
- Canvas 또는 LazyVerticalGrid 7열×N행
- level 색상 4단계 (Primary alpha 0.1/0.3/0.6/1.0)
- 월 네비게이션 + summary 카드
- ActivityHeatmapViewModelTest

### 완료 기준
- [ ] 3개 피처 UI + API
- [ ] 빌드 PASS: ./gradlew testDebugUnitTest
- [ ] 테스트 숫자 보고 (before: 968 → after)

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R3-android] feat: 설명 (한글), 파일 10개 이하

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[android 커밋] WO-XXX 해시 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[android 완료]' Enter
```

---

## 23:07:57 → ios

```
[TMA 마이그레이션] Phase 1: Core 모듈 TMA 분리

## 배경
토스의 Microfeatures (TMA) 아키텍처를 Aidy iOS에 적용한다.
Tuist/ProjectDescriptionHelpers/Module.swift 에 TMA 헬퍼가 이미 추가됨.

## 컨텍스트 로드
1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-ios/Tuist/ProjectDescriptionHelpers/Module.swift — TMA 헬퍼 읽기
3. ~/Develop/aidy-ios/Project.swift — 현재 구조 이해
4. ~/Develop/aidy-ios/Projects/App/Sources/Core/ — 현재 Core 파일 위치

## 작업

### Step 1: Core 모듈 디렉토리 생성 + 파일 이동
현재 Projects/App/Sources/Core/ 아래 14개 폴더를 TMA 구조로 분리:

주요 Core 모듈 (Interface 필요):
- Network → Projects/Core/Network/{Sources,Interface,Tests,Testing}/
- Model → Projects/Core/Model/{Sources,Interface,Tests,Testing}/

나머지 Core 유틸 (Interface 불필요, 단순 모듈):
- Haptics, L10n, Metrics, ErrorLog, AppInfo, Biometric, Draft, Notification, Pasteboard, SearchHistory
→ 각각 Projects/Core/{Name}/Sources/ 로 이동

### Step 2: Project.swift 수정
Module 헬퍼를 사용하여 Core 모듈들을 정의:

Welcome to Swift!

Subcommands:

  swift build      Build Swift packages
  swift package    Create and work on packages
  swift run        Run a program from a package
  swift test       Run package tests
  swift repl       Experiment with Swift code interactively

  Use `swift --version` for Swift version information.

  Use `swift --help` for descriptions of available options and flags.

  Use `swift help <subcommand>` for more information about a subcommand.

### Step 3: Feature 코드의 Core import 수정
기존: Core 폴더 내 직접 참조
변경: import Network, import Model 등 모듈별 import

### 완료 기준
- [ ] Core 모듈 디렉토리 TMA 구조로 재배치
- [ ] Project.swift에 Core 모듈 정의
- [ ] tuist generate --no-open 성공
- [ ] xcodebuild build-for-testing 성공

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [TMA] type: 설명 (한글), 파일 10개 이하
- 빌드 에러 시 import 수정에 집중
- 기존 기능 변경 없음 (구조만 변경)

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[ios 커밋] 해시 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```
