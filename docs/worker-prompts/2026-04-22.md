# 워커 프롬프트 로그 — 2026-04-22

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 00:22:44 → server

```
/exit
```

---

## 00:23:27 → ios

```
/exit
```

---

## 00:24:10 → android

```
/exit
```

---

## 00:42:36 → server

```
[R1] 작업 지시 (WO-154)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §8 Test Account
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-154-server-test-account-seed.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-server.md

---

## 작업 — UI 테스트 전용 계정 시딩 (WO-154)

### 구현
- application.yml에 aidy.test-account.enabled: false 추가
- application-test.yml에 aidy.test-account.enabled: true
- TestAccountSeeder (ApplicationRunner, @ConditionalOnProperty)
- 계정: uitest@aidy.com / AidyTest2026! / Aidy 테스터
- 멱등: 이미 존재하면 스킵

### 완료 기준
- [ ] 빌드 PASS
- [ ] 테스트: 시딩 동작 + 멱등성 + 프로덕션 미실행
- [ ] 테스트 숫자 보고: ./gradlew test

---

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-server] type: 설명, 한글, 1건당 파일 10개 이하

## 완료 보고
tmux send-keys -t aidy:0.0 '[server 완료]' Enter
```

---

## 00:43:31 → ios

```
[R1] 작업 지시 (WO-155)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §8 Test Account
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-155-ios-smoke-test-robot.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-ios.md

---

## 작업 — iOS Smoke Test + Robot Pattern (WO-155)

### 구현
1. Robot 클래스 5개 (Projects/App/UITests/Robots/):
   - BaseRobot, LoginRobot, ChatRobot, PeopleRobot, MemoryRobot
2. 테스트 계정: uitest@aidy.com / AidyTest2026! (LoginRobot에 하드코딩)
3. SmokeTest (Projects/App/UITests/SmokeTests/SmokeTest_CoreFlow.swift):
   로그인 → 채팅 전송 → AI 응답 → 기억 확인 → 인물 탭 → 프로필
4. 기존 UITest 리팩터링: loginIfNeeded() → LoginRobot 사용

### 완료 기준
- [ ] tuist build PASS
- [ ] Robot 5개 + SmokeTest 1개 생성
- [ ] 기존 UITest가 Robot 사용으로 리팩터링

---

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-ios] type: 설명, 한글, 1건당 파일 10개 이하

## 완료 보고
tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 00:44:24 → android

```
[R1] 작업 지시 (WO-156)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/specs/api-contract.md — §8 Test Account
3. ~/Develop/aidy-architect/specs/conventions.md
4. ~/Develop/aidy-architect/work-orders/in-progress/WO-156-android-smoke-test-robot.md
5. ~/Develop/aidy-architect/gates/test-policy.md + gates/test-policy-android.md

---

## 작업 — Android Smoke Test + Robot Pattern (WO-156)

### 구현
1. Robot 클래스 5개 (app/src/androidTest/java/com/mino/aidy/robots/):
   - BaseRobot, LoginRobot, ChatRobot, PeopleRobot, MemoryRobot
   - ComposeTestRule 기반
2. 테스트 계정: uitest@aidy.com / AidyTest2026! (LoginRobot에 하드코딩)
3. SmokeTest (app/src/androidTest/java/com/mino/aidy/smoke/SmokeTest_CoreFlow.kt):
   createAndroidComposeRule<MainActivity>() 사용
   로그인 → 채팅 전송 → AI 응답 → 기억 확인 → 인물 탭 → 프로필
4. 기존 UITest 리팩터링: 반복 로그인 코드 → LoginRobot 사용

### 완료 기준
- [ ] ./gradlew testDebugUnitTest PASS + 숫자 보고
- [ ] Robot 5개 + SmokeTest 1개 생성
- [ ] 기존 UITest가 Robot 사용으로 리팩터링

---

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-android] type: 설명, 한글, 1건당 파일 10개 이하

## 완료 보고
tmux send-keys -t aidy:0.0 '[android 완료]' Enter
```

---

## 16:56:15 → server

```
[R1] 작업 지시 (WO-157)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-server/CLAUDE.md
2. ~/Develop/aidy-architect/work-orders/in-progress/WO-157-ci-smoke-schedule.md

## 작업
GitHub Actions CI Smoke Test 워크플로 생성.

### 구현 사항
1. .github/workflows/smoke-test.yml 생성:
   - schedule: cron '0 21 * * *' (매일 06:00 KST)
   - workflow_dispatch (수동 실행)
   - ubuntu-latest 러너
   - setup-java 21 (temurin)
   - ./gradlew test 실행
   - 실패 시 test-report 아티팩트 업로드 (build/reports/tests/)
   - 실패 시 GitHub Issue 자동 생성 (peter-evans/create-issue-from-file 또는 단순 gh issue create)

2. 테스트 프로파일에서 테스트 계정 시딩 확인 (WO-154에서 이미 구현됨)

### 완료 기준
- [ ] smoke-test.yml 문법 유효 (actionlint 없으면 YAML 검증)
- [ ] 빌드 PASS: ./gradlew test
- [ ] 테스트 숫자 보고

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-server] type: 설명 (한글), 파일 10개 이하

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[server 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[server 완료]' Enter
```

---

## 16:57:37 → ios

```
[R1] 작업 지시 (WO-157)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-ios/CLAUDE.md
2. ~/Develop/aidy-architect/work-orders/in-progress/WO-157-ci-smoke-schedule.md

## 작업
GitHub Actions CI Smoke Test 워크플로 생성.

### 구현 사항
1. .github/workflows/smoke-test.yml 생성:
   - schedule: cron '0 21 * * *' (매일 06:00 KST = 21:00 UTC)
   - workflow_dispatch (수동 실행)
   - self-hosted runner (기존 test.yml과 동일: [self-hosted, macOS, ARM64, aidy-ios])
   - tuist install → tuist generate --no-open
   - xcodebuild test: SmokeTests 스킴/타겟만 실행 (테스트 필터)
     - -only-testing:AidyUITests/SmokeTests 또는 해당 타겟
   - 실패 시 TestResults.xcresult + 스크린샷 아티팩트 업로드
   - 실패 시 gh issue create로 이슈 자동 생성

2. 기존 test.yml의 러너/Tuist 설정 참조 (.github/workflows/test.yml)

### 완료 기준
- [ ] smoke-test.yml YAML 문법 유효
- [ ] tuist generate 통과 확인
- [ ] 빌드 PASS: xcodebuild build-for-testing

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-ios] type: 설명 (한글), 파일 10개 이하

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[ios 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 16:58:30 → android

```
[R1] 작업 지시 (WO-157)

## 컨텍스트 로드 (순서대로)
1. ~/Develop/aidy-android/CLAUDE.md
2. ~/Develop/aidy-architect/work-orders/in-progress/WO-157-ci-smoke-schedule.md

## 작업
GitHub Actions CI Smoke Test 워크플로 생성.

### 구현 사항
1. .github/workflows/smoke-test.yml 생성:
   - schedule: cron '0 21 * * *' (매일 06:00 KST = 21:00 UTC)
   - workflow_dispatch (수동 실행)
   - 기존 test.yml 패턴 참조 (gh-hosted primary + self-hosted fallback)
   - setup-java 21 (temurin) + ./gradlew testDebugUnitTest
   - connectedDebugAndroidTest --tests '*.smoke.*' (Smoke 테스트만)
     - 에뮬레이터 없으면 Unit Test만으로 대체 가능 (smoke 클래스가 unit test이면)
   - 실패 시 test-report 아티팩트 업로드
   - 실패 시 gh issue create로 이슈 자동 생성

2. 기존 test.yml의 러너/Gradle 설정 참조

### 완료 기준
- [ ] smoke-test.yml YAML 문법 유효
- [ ] 빌드 PASS: ./gradlew testDebugUnitTest
- [ ] 테스트 숫자 보고

## 제약
- 금지: git push, 새 패키지 설치
- 커밋: [R1-android] type: 설명 (한글), 파일 10개 이하

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[android 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[android 완료]' Enter
```

---

## 17:01:52 → server

```
[R2] 작업 지시: 누락 컨트롤러 테스트 추가

## 컨텍스트
1. ~/Develop/aidy-server/CLAUDE.md
2. 기존 컨트롤러 테스트 참조: src/test/kotlin/com/mino/aidy/controller/AuthControllerTest.kt (패턴 참고)

## 작업
누락된 6개 컨트롤러의 단위 테스트 추가:
1. ReminderController — GET /api/reminders, POST /api/reminders/{memoryId}/dismiss
2. SummaryController — GET /api/summary/weekly
3. ChatSuggestionController — GET /api/chat/suggestions, POST /api/chat/suggestions/{id}/use
4. SharedController — POST /api/memories/{id}/share (v3.2), GET /api/shared/{shareId}, DELETE /api/memories/{id}/share
5. TagController — GET/POST/DELETE /api/tags, POST/DELETE /api/memories/{id}/tags
6. RelationshipTimelineController — GET /api/people/{personId}/timeline

각 컨트롤러마다:
- Happy path 최소 1개
- 에러 케이스 (404, 400) 최소 1개
- @WebMvcTest 또는 기존 테스트 패턴 따르기

### 완료 기준
- [ ] 6개 테스트 파일 생성
- [ ] 빌드 PASS: ./gradlew test
- [ ] 테스트 숫자 증가 보고 (before: 1001)

## 제약
- 금지: git push, 기존 코드 수정, 새 패키지 설치
- 커밋: [R2-server] test: 설명 (한글), 파일 10개 이하
- 컨트롤러당 1커밋 또는 묶어서 2커밋

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[server 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[server 완료]' Enter
```

---

## 17:03:13 → ios

```
[R2] 작업 지시: 누락 TCA Feature 테스트 추가

## 컨텍스트
1. ~/Develop/aidy-ios/CLAUDE.md
2. 기존 Feature 테스트 참조: Projects/App/Tests/ChatFeatureTests.swift (패턴 참고)

## 작업
누락된 9개 TCA Feature의 단위 테스트 추가:
1. TagFeature
2. MemoryShareFeature
3. QuickNoteFeature
4. ChatExportFeature
5. HealthSummaryFeature
6. PersonMergeFeature
7. RelationshipTimelineFeature
8. RelationshipHealthFeature
9. PersonDetailFeature

각 Feature마다:
- TestStore 사용
- Happy path 최소 1개 (주요 액션 → 상태 변경 확인)
- 에러 케이스 최소 1개 (API 실패 시 에러 상태)
- 기존 테스트 패턴 (withDependencies, exhaustivity) 따르기

### 완료 기준
- [ ] 9개 테스트 파일 생성
- [ ] 빌드 PASS: xcodebuild build-for-testing
- [ ] 테스트 숫자 증가 보고

## 제약
- 금지: git push, 기존 코드 수정, 새 패키지 설치
- 커밋: [R2-ios] test: 설명 (한글), 파일 10개 이하
- 3커밋 이내 (3개씩 묶기)

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[ios 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[ios 완료]' Enter
```

---

## 17:04:06 → android

```
[R2] 작업 지시: 누락 ViewModel 테스트 + Repository 테스트 보강

## 컨텍스트
1. ~/Develop/aidy-android/CLAUDE.md
2. 기존 ViewModel 테스트 참조: app/src/test/java/com/mino/aidy/ui/chat/ChatViewModelTest.kt (패턴 참고)

## 작업
1. SearchViewModel 테스트 추가 (누락 1개):
   - 검색 성공/빈 결과/에러 케이스
   
2. Repository 테스트 보강 — 아래 중 테스트 없는 Repository 확인 후 추가:
   - 누락된 Repository 테스트 찾기: find app/src/main/ -name '*Repository.kt' 후 test/ 대조
   - 각 Repository마다: happy path + 에러 케이스 (mock API response)

### 완료 기준
- [ ] SearchViewModelTest 생성
- [ ] 누락 Repository 테스트 추가 (있으면)
- [ ] 빌드 PASS: ./gradlew testDebugUnitTest
- [ ] 테스트 숫자 증가 보고 (before: 869)

## 제약
- 금지: git push, 기존 코드 수정, 새 패키지 설치
- 커밋: [R2-android] test: 설명 (한글), 파일 10개 이하

## 보고
커밋 직후: tmux send-keys -t aidy:0.0 '[android 커밋] abc1234 설명' Enter
완료 후: tmux send-keys -t aidy:0.0 '[android 완료]' Enter
```
