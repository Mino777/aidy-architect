# 워커 프롬프트 로그 — 2026-04-18

> architect-cli.sh `send` 로 워커에게 보낸 프롬프트 원문. 프롬프트 엔지니어링 학습/회고용.


---

## 00:00:58 → android

```
[R2-s18] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Rate Limit 헤더 (v0.9)
3. 작업:
   - AidyApiService/ErrorHandler에서 429 응답 시 Retry-After 헤더 파싱
   - 429 에러 발생 시 남은 대기 시간 표시 (예: 'N초 후 재시도 가능')
   - 재시도 버튼을 Retry-After 만큼 비활성화 후 자동 활성화
   - ChatScreen에서 rate limit 시 사용자 친화적 메시지 표시
   - 테스트
커밋: [R2-android] feat: Rate limit 재시도 UX (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:05:59 → server

```
[R3-s18] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/import (v0.9)
3. 작업:
   - MemoryService에 importMemories(userId, List<ImportItem>) 추가
   - 중복 판정: 동일 userId + title + content + category → skip
   - MemoryController에 POST /api/memories/import 추가
   - 최대 200건 제한
   - imported/skipped/total 응답
   - 테스트: 단위 + E2E (정상 import, 중복 skip, validation 에러)
커밋: [R3-server] feat: Memory import API (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:13:11 → ios

```
[R3-s18] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/import (v0.9)
3. 작업:
   - APIClient에 importMemories(request) 추가
   - Settings 화면에 '메모리 가져오기' 버튼 추가
   - 파일 선택 (fileImporter) → JSON 파싱 → POST /api/memories/import 호출
   - 결과 표시: N건 가져옴, M건 건너뜀
   - export 포맷과 동일 구조이므로 export JSON 그대로 import 가능
   - 테스트
커밋: [R3-ios] feat: Memory import (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:13:55 → android

```
[R3-s18] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/import (v0.9)
3. 작업:
   - AidyApiService에 importMemories(request) 추가
   - Settings 화면에 '메모리 가져오기' 버튼 추가
   - 파일 선택 (ActivityResultContracts.OpenDocument) → JSON 파싱 → POST 호출
   - 결과 표시: N건 가져옴, M건 건너뜀
   - export 포맷과 동일 구조
   - 테스트
커밋: [R3-android] feat: Memory import (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:25:13 → server

```
[R4-s18] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/summary (v0.9)
3. 작업:
   - ChatService에 getSummary(userId) 추가
   - 최근 24시간 메시지 수집 → AiService로 요약 생성 요청
   - 요약 프롬프트: '다음 대화를 3줄 이내로 요약하고, 주요 토픽을 한국어 키워드로 추출해줘'
   - 응답: { period, messageCount, summary, topics[], memoriesCreated }
   - 캐시: 1시간 in-memory (ConcurrentHashMap<userId, CachedSummary>)
   - 대화 없으면 기본 응답
   - Circuit Breaker OPEN → 503 AI_UNAVAILABLE
   - ChatController에 GET /api/chat/summary 추가
   - 테스트: 단위 + E2E (요약 생성, 캐시 히트, 빈 대화, CB OPEN)
커밋: [R4-server] feat: Chat summary API (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:31:22 → ios

```
[R4-s18] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/summary (v0.9)
3. 작업:
   - APIClient에 fetchChatSummary() 추가
   - ChatSummary 모델 (period, messageCount, summary, topics, memoriesCreated)
   - Chat 화면 상단에 오늘의 요약 카드 표시 (접기/펼치기)
   - 요약 로딩 중 shimmer/skeleton
   - 테스트
커밋: [R4-ios] feat: Chat summary (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:32:06 → android

```
[R4-s18] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/summary (v0.9)
3. 작업:
   - AidyApiService에 getChatSummary() 추가
   - ChatSummaryResponse 모델 (period, messageCount, summary, topics, memoriesCreated)
   - Chat 화면 상단에 오늘의 요약 카드 표시 (접기/펼치기)
   - 요약 로딩 중 shimmer/placeholder
   - 테스트
커밋: [R4-android] feat: Chat summary (v0.9)
커밋 1건당 파일 10개 이하.
```

---

## 00:41:36 → ios

```
[R5-s18] 품질 보강:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 전체 테스트 실행 + 빌드 green 확인
3. R1~R4에서 추가한 테마/rate limit/import/summary에 테스트 갭 있으면 보강
4. 보강 건수 + 전체 테스트 수 보고
커밋: [R5-ios] test: 테스트 보강 + 빌드 검증
커밋 1건당 파일 10개 이하.
```

---

## 00:43:37 → android

```
[R5-s18] 품질 보강:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 전체 테스트 실행 + 빌드 green 확인
3. R1~R4에서 추가한 테마/rate limit/import/summary에 테스트 갭 있으면 보강
4. 보강 건수 + 전체 테스트 수 보고
커밋: [R5-android] test: 테스트 보강 + 빌드 검증
커밋 1건당 파일 10개 이하.
```

---

## 00:46:49 → ios

```
[R5-s18] 품질 보강:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 전체 테스트 실행 + 빌드 green 확인
3. R1~R4에서 추가한 테마/rate limit/import/summary에 테스트 갭 있으면 보강
4. 보강 건수 + 전체 테스트 수 보고
커밋: [R5-ios] test: 테스트 보강 + 빌드 검증
커밋 1건당 파일 10개 이하.
```

---

## 00:48:49 → android

```
[R5-s18] 품질 보강:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 전체 테스트 실행 + 빌드 green 확인
3. R1~R4에서 추가한 테마/rate limit/import/summary에 테스트 갭 있으면 보강
4. 보강 건수 + 전체 테스트 수 보고
커밋: [R5-android] test: 테스트 보강 + 빌드 검증
커밋 1건당 파일 10개 이하.
```

---

## 11:38:18 → server

```
[R1-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.5 Memory Timeline (v1.0)
3. 작업:
   - MemoryService에 getTimeline(userId, days) 추가
   - MemoryController에 GET /api/memories/timeline?days=7 추가
   - 날짜별 GROUP BY, 최신 먼저, 각 날짜 내 최신순
   - days 기본 7, 최대 30, 0이하 400 VALIDATION_ERROR
   - 메모리 없는 날짜 생략
   - 테스트: 단위 + E2E
커밋: [R1-server] feat: Memory timeline API (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:44:30 → ios

```
[R1-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.5 Memory Timeline (v1.0)
3. 작업:
   - APIClient에 fetchMemoryTimeline(days:) 추가
   - MemoryTimeline 모델 정의
   - Memory 탭에 타임라인 뷰 추가 (날짜별 섹션 헤더 + 메모리 목록)
   - 기본 7일, 더보기 시 30일
   - 테스트
커밋: [R1-ios] feat: Memory timeline (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:45:14 → android

```
[R1-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.5 Memory Timeline (v1.0)
3. 작업:
   - AidyApiService에 getMemoryTimeline(days) 추가
   - MemoryTimelineResponse 모델 정의
   - Memory 화면에 타임라인 뷰 추가 (날짜별 섹션 헤더 + 메모리 목록)
   - 기본 7일, 더보기 시 30일
   - 테스트
커밋: [R1-android] feat: Memory timeline (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:52:44 → server

```
[R2-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/quickactions (v1.0)
3. 작업:
   - ChatController에 GET /api/chat/quickactions 추가
   - 6개 고정 퀵 액션 반환 (daily_expense, schedule_check, mood_log, work_memo, health_log, people_note)
   - 각 액션: id, emoji, label, prompt 필드
   - 테스트
커밋: [R2-server] feat: Quick actions API (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:53:27 → ios

```
[R2-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/quickactions (v1.0)
3. 작업:
   - APIClient에 fetchQuickActions() 추가
   - QuickAction 모델 (id, emoji, label, prompt)
   - ChatView 채팅 입력 위에 가로 스크롤 퀵 버튼 추가
   - 탭 시 prompt를 입력에 채우고 자동 전송
   - 테스트
커밋: [R2-ios] feat: Quick actions (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:54:11 → android

```
[R2-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — GET /api/chat/quickactions (v1.0)
3. 작업:
   - AidyApiService에 getQuickActions() 추가
   - QuickAction 모델 (id, emoji, label, prompt)
   - ChatScreen 채팅 입력 위에 가로 스크롤 퀵 버튼 추가 (LazyRow)
   - 탭 시 prompt를 입력에 채우고 자동 전송
   - 테스트
커밋: [R2-android] feat: Quick actions (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 11:59:59 → server

```
[R3-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/{id}/share + GET /api/shared/{token} (v1.0)
3. 작업:
   - SharedMemory Entity (id, memoryId, shareToken, expiresAt, createdAt)
   - SharedMemoryRepository
   - ShareService — createShare(userId, memoryId) + getShared(token)
   - MemoryController에 POST /api/memories/{id}/share
   - 별도 SharedController에 GET /api/shared/{token} (인증 불필요!)
   - 토큰: 32자 URL-safe, 24시간 유효
   - 테스트: 단위 + E2E
커밋: [R3-server] feat: Memory share API (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:07:10 → ios

```
[R3-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/{id}/share (v1.0)
3. 작업:
   - APIClient에 shareMemory(id:) 추가
   - Memory 상세/리스트에 공유 버튼 추가
   - 공유 성공 시 shareUrl을 시스템 공유 시트로 표시 (UIActivityViewController)
   - 테스트
커밋: [R3-ios] feat: Memory 공유 (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:07:53 → android

```
[R3-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — POST /api/memories/{id}/share (v1.0)
3. 작업:
   - AidyApiService에 shareMemory(id) 추가
   - Memory 리스트에 공유 버튼 추가
   - 공유 성공 시 shareUrl을 Intent.ACTION_SEND로 공유
   - 테스트
커밋: [R3-android] feat: Memory 공유 (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:15:39 → server

```
[R4-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.4 Reminders (v1.0)
3. 작업:
   - ReminderService: schedule 카테고리 메모리에서 날짜 추출 (YYYY-MM-DD regex)
   - 오늘 이후 + dismissed 아닌 것만 필터
   - GET /api/reminders — 가까운 날짜 먼저 정렬, daysUntil 계산
   - POST /api/reminders/{memoryId}/dismiss — Memory에 dismissed 필드 추가
   - Memory Entity에 dismissed Boolean 필드 추가 (default false)
   - 테스트: 단위 + E2E
커밋: [R4-server] feat: Reminders API (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:20:44 → ios

```
[R4-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.4 Reminders (v1.0)
3. 작업:
   - APIClient에 fetchReminders() + dismissReminder(memoryId:) 추가
   - 홈 또는 Memory 탭에 알림 섹션 추가 (오늘/다가오는 일정)
   - dismiss 버튼 (스와이프 또는 탭)
   - 테스트
커밋: [R4-ios] feat: Reminders (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:21:28 → android

```
[R4-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 5.4 Reminders (v1.0)
3. 작업:
   - AidyApiService에 getReminders() + dismissReminder(memoryId) 추가
   - Memory 화면에 알림 섹션 추가 (오늘/다가오는 일정)
   - dismiss 버튼 (스와이프)
   - 테스트
커밋: [R4-android] feat: Reminders (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:29:13 → server

```
[R5-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 7. App Config (v1.0)
3. 작업:
   - AppConfigController (인증 불필요) — GET /api/app/config
   - 하드코딩 응답: minVersion=1.0.0, latestVersion=1.0.0, forceUpdate=false, features 전부 true
   - Health 버전도 1.0.0으로 업데이트
   - 테스트
커밋: [R5-server] feat: App config API (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:29:56 → ios

```
[R5-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 7. App Config (v1.0)
3. 작업:
   - APIClient에 fetchAppConfig() 추가 (인증 불필요)
   - AppConfig 모델
   - 앱 시작 시 config 조회 → forceUpdate=true면 업데이트 필수 알림
   - features 플래그로 UI 표시/숨김
   - 테스트
커밋: [R5-ios] feat: App config (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:30:39 → android

```
[R5-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 7. App Config (v1.0)
3. 작업:
   - AidyApiService에 getAppConfig() 추가 (인증 불필요)
   - AppConfigResponse 모델
   - 앱 시작 시 config 조회 → forceUpdate=true면 업데이트 필수 다이얼로그
   - features 플래그로 UI 표시/숨김
   - 테스트
커밋: [R5-android] feat: App config (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:35:51 → ios

```
[R5-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — Section 7. App Config (v1.0)
3. 작업:
   - APIClient에 fetchAppConfig() 추가 (인증 불필요)
   - AppConfig 모델 (minVersion, latestVersion, forceUpdate, announcement, features)
   - 앱 시작 시 config 조회 → forceUpdate=true면 업데이트 필수 알림
   - features 플래그로 UI 표시/숨김
   - 테스트
커밋: [R5-ios] feat: App config (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:41:28 → server

```
[R6-s19] 작업 지시:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — PATCH /api/memories/{id}/tags (v1.0)
3. 작업:
   - Memory Entity에 tags 필드 추가 (JSON 문자열 또는 ElementCollection)
   - MemoryController에 PATCH /api/memories/{id}/tags 추가
   - GET /api/memories 응답에 tags 필드 포함
   - GET /api/memories?tag=중요 필터 지원
   - 태그 최대 10개, 각 1~20자 validation
   - 테스트
커밋: [R6-server] feat: Memory custom tags (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:47:11 → ios

```
[R6-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — PATCH /api/memories/{id}/tags (v1.0)
3. 작업:
   - APIClient에 updateTags(memoryId:, tags:) 추가
   - Memory 모델에 tags: [String] 추가
   - Memory 상세에서 태그 편집 UI (태그 칩 + 추가/삭제)
   - 테스트
커밋: [R6-ios] feat: Memory custom tags (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:47:55 → android

```
[R6-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 — PATCH /api/memories/{id}/tags (v1.0)
3. 작업:
   - AidyApiService에 updateTags(memoryId, tags) 추가
   - Memory 모델에 tags: List<String> 추가
   - Memory 리스트에서 태그 칩 표시 + 태그 편집 다이얼로그
   - 테스트
커밋: [R6-android] feat: Memory custom tags (v1.0)
커밋 1건당 파일 10개 이하.
```

---

## 12:55:22 → ios

```
[R7-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 작업:
   - AI 응답(assistant role) 메시지에 마크다운 렌더링 적용
   - 볼드(**text**), 이탤릭(*text*), 코드(`code`), 리스트(- item) 지원
   - 기존 라이브러리 사용 또는 AttributedString 직접 파싱
   - 새 패키지 설치 금지 — 기존 라이브러리나 SwiftUI 내장 기능만 사용
   - user 메시지는 plain text 유지
   - 테스트
커밋: [R7-ios] feat: Chat markdown 렌더링
커밋 1건당 파일 10개 이하.
```

---

## 12:56:05 → android

```
[R7-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 작업:
   - AI 응답(assistant role) 메시지에 마크다운 렌더링 적용
   - 볼드(**text**), 이탤릭(*text*), 코드(`code`), 리스트(- item) 지원
   - 새 패키지 설치 금지 — AnnotatedString 직접 파싱 또는 기존 의존성 사용
   - user 메시지는 plain text 유지
   - 테스트
커밋: [R7-android] feat: Chat markdown 렌더링
커밋 1건당 파일 10개 이하.
```

---

## 13:01:24 → ios

```
[R8-s19] 작업 지시:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 작업:
   - 모든 사용자 표시 문자열을 LocalizedStringKey 또는 String Catalog로 추출
   - ko (한국어) + en (영어) 2개 언어 지원
   - Settings의 language 설정 (ko/en)에 따라 앱 언어 전환
   - 주요 화면: Chat, Memory, Settings, Auth의 UI 텍스트
   - 새 패키지 설치 금지
   - 테스트
커밋: [R8-ios] feat: 다국어 지원 (ko/en)
커밋 1건당 파일 10개 이하.
```

---

## 13:02:07 → android

```
[R8-s19] 작업 지시:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. 작업:
   - 모든 사용자 표시 문자열을 strings.xml로 추출
   - values/strings.xml (ko) + values-en/strings.xml (en) 구성
   - Settings의 language 설정 (ko/en)에 따라 앱 언어 전환 (AppCompatDelegate.setApplicationLocales)
   - 주요 화면: Chat, Memory, Settings, Auth의 UI 텍스트
   - 새 패키지 설치 금지
   - 테스트
커밋: [R8-android] feat: 다국어 지원 (ko/en)
커밋 1건당 파일 10개 이하.
```

---

## 13:29:05 → server

```
[R9-s19] 테스트 보강:
1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. R1~R6에서 추가한 timeline/quickactions/share/reminders/appconfig/tags에 대해 E2E 테스트 갭 확인
3. 갭 발견 시 추가 작성
4. 전체 테스트 실행 + green 확인
커밋: [R9-server] test: E2E 테스트 보강
커밋 1건당 파일 10개 이하.
```

---

## 13:29:49 → ios

```
[R9-s19] 테스트 보강:
1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. R1~R8에서 추가한 기능에 대해 테스트 갭 확인
3. 갭 발견 시 추가 작성
4. 전체 빌드 + 테스트 green 확인
커밋: [R9-ios] test: 테스트 보강
커밋 1건당 파일 10개 이하.
```

---

## 13:30:33 → android

```
[R9-s19] 테스트 보강:
1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. R1~R8에서 추가한 기능에 대해 테스트 갭 확인
3. 갭 발견 시 추가 작성
4. 전체 빌드 + 테스트 green 확인
커밋: [R9-android] test: 테스트 보강
커밋 1건당 파일 10개 이하.
```

---

## 13:39:35 → server

```
/exit
```

---

## 13:40:19 → ios

```
/exit
```

---

## 13:41:02 → android

```
/exit
```

---

## 17:18:55 → server

```
[R1] 작업 지시 — Unit 테스트 갭 보강

1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기
3. docs/api-catalog.md에서 테스트 갭 확인

작업: 다음 엔드포인트의 Unit 테스트를 추가하라.

A. GET /api/chat/stats — ChatControllerTest에 추가
   - 정상 응답 검증 (totalMessages, userMessages, dailyAverage 등)
   - 메시지 0건일 때 기본값 검증

B. GET /api/memories/search — MemoryControllerTest에 추가
   - 키워드 검색 정상 동작
   - 빈 q 파라미터 → 400 VALIDATION_ERROR
   - 결과 없을 때 빈 배열

C. DELETE /api/memories/{id} — MemoryControllerTest에 추가
   - 정상 삭제 → 204
   - 없는 ID → 404 MEMORY_NOT_FOUND
   - 타인 메모리 → 403 FORBIDDEN

D. GET /api/memories/categories — MemoryControllerTest에 추가
   - 카테고리별 count 정상 반환
   - 메모리 0건 시 빈 categories

E. POST /api/memories/batch — MemoryControllerTest에 추가
   - delete 액션 정상 동작
   - pin/unpin 액션 정상 동작
   - 빈 memoryIds → 400

F. GET /api/search — SearchControllerTest 신규 생성 또는 기존에 추가
   - 통합 검색 정상 동작 (chat + memories + people 결과)
   - 빈 q → 400

커밋 메시지: [R1-server] test: Unit 테스트 갭 보강
커밋 1건당 파일 10개 이하.
기존 테스트 패턴 참고하여 일관된 스타일로 작성.
완료되면 idle 상태로 대기.
```

---

## 17:27:22 → android

```
[R2] 작업 지시 — 고급 기능 UI 완성도 검증 + 수정

1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기

작업: 다음 v1.0 기능의 UI가 올바르게 구현되었는지 확인하고, 미흡한 부분을 수정하라.

A. Memory Insights (GET /api/memories/insights)
   - 카테고리 분포 차트/리스트가 화면에 표시되는지 확인
   - weeklyActivity 표시되는지
   - streakDays, mostActiveCategory 표시

B. Memory Timeline (GET /api/memories/timeline)
   - 날짜별 그룹핑 UI가 있는지
   - 날짜 헤더 + 메모리 리스트 형태인지

C. Memory Share (POST /api/memories/{id}/share)
   - 공유 버튼이 메모리 상세에 있는지
   - shareUrl 복사/공유 기능

D. Reminders (GET /api/reminders)
   - 알림 목록 화면
   - dismiss 기능
   - daysUntil 표시

누락된 UI가 있으면 구현하라. 기존 디자인 패턴과 일관되게.
커밋 메시지: [R2-android] fix: 고급 기능 UI 완성도 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:28:50 → ios

```
[R2] 작업 지시 — 고급 기능 UI 완성도 검증 + 수정

1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기

작업: 다음 v1.0 기능의 UI가 올바르게 구현되었는지 확인하고, 미흡한 부분을 수정하라.

A. Memory Insights (GET /api/memories/insights)
   - InsightsView가 존재하고, categoryDistribution 시각화
   - weeklyActivity 차트/리스트
   - streakDays, mostActiveCategory 표시

B. Memory Timeline (GET /api/memories/timeline)
   - TimelineView가 존재하고 날짜별 그룹핑 UI
   - 날짜 헤더 + 메모리 카드 리스트

C. Memory Share (POST /api/memories/{id}/share)
   - 공유 버튼이 메모리 상세에 있는지
   - 공유 URL 복사 + iOS ShareSheet 연동

D. Reminders (GET /api/reminders)
   - RemindersView가 있고 알림 목록 표시
   - dismiss 기능 + daysUntil 배지

누락된 UI가 있으면 구현하라. TCA 패턴에 맞게.
커밋 메시지: [R2-ios] fix: 고급 기능 UI 완성도 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:40:43 → server

```
[R3] 작업 지시 — E2E 테스트 누락 보강

1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. docs/api-catalog.md에서 E2E ❌ 항목 확인

작업: 다음 엔드포인트의 E2E 테스트를 추가하라.

A. POST /api/auth/refresh — E2E 테스트
   - 유효한 토큰으로 refresh → 새 토큰 반환
   - 만료/무효 토큰 → 401

B. POST /api/memories/{id}/feedback — E2E 테스트
   - isCorrect: true → status: ok
   - isCorrect: false → status: deleted + 메모리 삭제 확인

C. GET /api/memories/people — E2E 테스트
   - 채팅으로 people 메모리 생성 후 조회
   - person 쿼리 없이 → 400

D. GET /api/memories/search — E2E 테스트
   - 키워드로 검색 → 결과 반환
   - 빈 q → 400

E. DELETE /api/memories/{id} — E2E 테스트
   - 정상 삭제 → 204
   - 없는 ID → 404

F. GET /api/memories/categories — E2E 테스트
   - 카테고리별 count 확인

G. GET /api/health — E2E 테스트
   - status: ok 확인

기존 E2E 테스트 파일 패턴 참고 (ChatE2ETest 등).
커밋 메시지: [R3-server] test: E2E 테스트 누락 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:47:45 → ios

```
[R4] 작업 지시 — SSE 스트리밍 안정성 + 에러 핸들링 보강

1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (POST /api/chat/stream 참조)

작업:

A. SSE 스트리밍 에러 핸들링 검증 + 보강
   - event: error 수신 시 적절한 에러 UI 표시 확인
   - 네트워크 끊김 시 graceful 처리 (재시도 버튼 등)
   - SseEmitter timeout (30초) 후 처리
   - Circuit Breaker OPEN (AI_UNAVAILABLE) 시 사용자 메시지

B. 401 UNAUTHORIZED 자동 처리
   - JWT 만료 시 자동 로그아웃 + 로그인 화면 이동 확인
   - 모든 API 호출에 일관된 401 처리가 있는지 확인
   - 누락된 곳이 있으면 추가

C. Rate Limit 처리
   - 429 수신 시 Retry-After 활용 확인
   - 사용자에게 '잠시 후 다시 시도' 메시지 표시

수정이 필요하면 테스트도 함께 추가.
커밋 메시지: [R4-ios] fix: 에러 핸들링 + SSE 안정성 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:49:57 → android

```
[R4] 작업 지시 — Repository 테스트 추가

1. ~/Develop/aidy-android/CLAUDE.md 읽기

작업: Repository 레이어 Unit 테스트를 추가하라.

A. ChatRepository 테스트
   - sendMessage 성공/실패 케이스
   - getHistory 정상 반환
   - deleteMessage 정상/실패
   - searchHistory 키워드 검색

B. MemoryRepository 테스트
   - getMemories 페이지네이션
   - updateMemory 수정
   - deleteMemory 삭제
   - pinMemory 핀 토글
   - batchAction 일괄 처리
   - getInsights 반환 검증
   - getTimeline 반환 검증

C. PeopleRepository 테스트
   - getPeople 조회
   - giveFeedback 피드백 전송

기존 테스트 패턴(ViewModel 테스트에서 mock 방식) 참고.
Retrofit API 호출은 mock하여 테스트.
커밋 메시지: [R4-android] test: Repository 레이어 테스트 추가
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:52:58 → ios

```
[R4] 작업 지시 — SSE 스트리밍 안정성 + 에러 핸들링 보강

1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기 (POST /api/chat/stream 참조)

작업:

A. SSE 스트리밍 에러 핸들링 검증 + 보강
   - event: error 수신 시 적절한 에러 UI 표시 확인
   - 네트워크 끊김 시 graceful 처리 (재시도 버튼 등)
   - SseEmitter timeout (30초) 후 처리
   - Circuit Breaker OPEN (AI_UNAVAILABLE) 시 사용자 메시지

B. 401 UNAUTHORIZED 자동 처리
   - JWT 만료 시 자동 로그아웃 + 로그인 화면 이동 확인
   - 모든 API 호출에 일관된 401 처리가 있는지 확인
   - 누락된 곳이 있으면 추가

C. Rate Limit 처리
   - 429 수신 시 Retry-After 활용 확인
   - 사용자에게 '잠시 후 다시 시도' 메시지 표시

수정이 필요하면 테스트도 함께 추가.
커밋 메시지: [R4-ios] fix: 에러 핸들링 + SSE 안정성 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 17:56:46 → server

```
[R5] 작업 지시 — API 카탈로그 v1.0 동기화 + 에러 핸들링

1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기

작업:

A. docs/api-catalog.md를 v1.0 기준으로 동기화
   - 현재 v0.6 기준 → v1.0까지 누락된 엔드포인트 추가:
     * PUT /api/auth/password (v0.7)
     * DELETE /api/auth/account (v0.7)
     * GET/PUT /api/settings (v0.7)
     * DELETE /api/chat/history (v0.8)
     * GET /api/chat/history (offset/limit 파라미터 추가)
     * GET /api/memories/insights (v0.8)
     * GET /api/memories/timeline (v1.0)
     * GET /api/chat/quickactions (v1.0)
     * POST /api/memories/{id}/share + GET /api/shared/{token} (v1.0)
     * GET /api/reminders + POST dismiss (v1.0)
     * GET /api/app/config (v1.0)
     * PATCH /api/memories/{id}/tags (v1.0)
     * GET /api/chat/summary (v0.9)
     * POST /api/memories/import (v0.9)
   - 기존 항목의 테스트 커버리지 표 업데이트 (R1, R3에서 추가한 테스트 반영)
   - 하단 요약표도 v1.0 기준으로 업데이트

B. 에러 핸들링 엣지케이스 보강
   - Controller에서 누락된 validation 체크 (null/empty 파라미터)
   - 존재하지 않는 엔드포인트 → 적절한 404 응답 확인

커밋 메시지: [R5-server] docs: API 카탈로그 v1.0 동기화
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:08:58 → ios

```
[R6] 작업 지시 — 접근성 + VoiceOver 보강

1. ~/Develop/aidy-ios/CLAUDE.md 읽기

작업:

A. VoiceOver 접근성 레이블 추가
   - 모든 버튼/아이콘에 accessibilityLabel 확인
   - 채팅 버블에 적절한 accessibilityLabel
   - 메모리 카드에 accessibilityLabel
   - 네비게이션 탭에 accessibilityLabel
   - 누락된 곳에 추가

B. Dynamic Type 대응
   - 시스템 폰트 크기 변경 시 레이아웃 깨지지 않는지 확인
   - @ScaledMetric 또는 .dynamicTypeSize 사용 확인

C. 색상 대비 접근성
   - 텍스트/배경 대비가 WCAG AA 기준 (4.5:1) 충족하는지 확인
   - 문제 있으면 수정

커밋 메시지: [R6-ios] a11y: 접근성 보강
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:09:50 → android

```
[R6] 작업 지시 — 접근성 + 에러 핸들링 일관성

1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기

작업:

A. 접근성 (TalkBack) 보강
   - 모든 버튼/아이콘에 contentDescription 확인
   - 채팅 버블에 적절한 semantics
   - 메모리 카드에 semantics
   - 누락된 곳에 추가

B. 401 UNAUTHORIZED 일관 처리
   - 모든 API 호출에서 401 수신 시 자동 로그아웃 + 로그인 화면
   - Repository/ViewModel 레벨에서 일관된 처리 확인
   - 누락된 곳에 추가

C. Rate Limit (429) 처리
   - 429 수신 시 사용자에게 '잠시 후 다시 시도' 메시지
   - Retry-After 헤더 활용

커밋 메시지: [R6-android] fix: 접근성 + 에러 핸들링 일관성
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:15:03 → android

```
[R6] 작업 지시 — 접근성 + 에러 핸들링 일관성

1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ~/Develop/aidy-architect/specs/api-contract.md 읽기

작업:

A. 접근성 (TalkBack) 보강
   - 모든 버튼/아이콘에 contentDescription 확인
   - 채팅 버블에 적절한 semantics
   - 메모리 카드에 semantics
   - 누락된 곳에 추가

B. 401 UNAUTHORIZED 일관 처리
   - 모든 API 호출에서 401 수신 시 자동 로그아웃 + 로그인 화면
   - Repository/ViewModel 레벨에서 일관된 처리 확인
   - 누락된 곳에 추가

C. Rate Limit (429) 처리
   - 429 수신 시 사용자에게 '잠시 후 다시 시도' 메시지
   - Retry-After 헤더 활용

커밋 메시지: [R6-android] fix: 접근성 + 에러 핸들링 일관성
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:17:39 → server

```
[R7] 작업 지시 — 쿼리 성능 최적화

1. ~/Develop/aidy-server/CLAUDE.md 읽기

작업:

A. JPA 쿼리 최적화
   - N+1 쿼리 문제 확인 (특히 Memory + Tags, Person + PersonMemory)
   - 필요시 @EntityGraph 또는 JOIN FETCH 추가
   - GET /api/memories (리스트) 조회 시 불필요한 eager loading 제거

B. 인덱스 확인
   - chat_messages 테이블: user_id + created_at 복합 인덱스 확인
   - memories 테이블: user_id + category 인덱스 확인
   - 누락된 인덱스 → Flyway 마이그레이션 추가 (새 파일만)

C. 캐싱 개선
   - GET /api/app/config 응답 캐싱 확인 (변경 거의 없는 데이터)
   - GET /api/memories/categories 캐싱 고려

커밋 메시지: [R7-server] perf: 쿼리 최적화 + 인덱스
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:18:32 → ios

```
[R7] 작업 지시 — 리스트 성능 최적화

1. ~/Develop/aidy-ios/CLAUDE.md 읽기

작업:

A. 메모리 리스트 최적화
   - LazyVStack 사용 확인 (큰 리스트에서 VStack 사용 금지)
   - 메모리 카드 View에 EquatableView 또는 적절한 id 사용으로 불필요한 re-render 방지

B. 채팅 리스트 최적화
   - 긴 대화에서 스크롤 성능 확인
   - LazyVStack + scrollTo 사용 확인
   - 마크다운 렌더링이 스크롤 시 반복 계산되지 않는지 확인

C. 이미지/아이콘 캐싱
   - SF Symbols 사용 시 적절한 렌더링 모드
   - 불필요한 상태 재계산 방지

커밋 메시지: [R7-ios] perf: 리스트 성능 최적화
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:19:24 → android

```
[R7] 작업 지시 — 리스트 성능 최적화

1. ~/Develop/aidy-android/CLAUDE.md 읽기

작업:

A. Compose 리스트 최적화
   - LazyColumn 사용 확인 (Column 사용 금지)
   - key 파라미터로 적절한 ID 사용 (item의 id 활용)
   - 메모리/채팅 카드가 불필요하게 recompose 되지 않도록 확인

B. 채팅 리스트 최적화
   - 긴 대화에서 스크롤 성능
   - remember {}로 마크다운 파싱 결과 캐싱 확인
   - scrollState 관리 적절한지

C. ViewModel 상태 최적화
   - MutableState 업데이트 시 불필요한 recomposition 방지
   - derivedStateOf 또는 snapshotFlow 활용 가능한 곳

커밋 메시지: [R7-android] perf: Compose 리스트 성능 최적화
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:26:11 → server

```
[R8] 작업 지시 — 보안 하드닝

1. ~/Develop/aidy-server/CLAUDE.md 읽기

작업:

A. 입력 검증 강화
   - 모든 String 입력에 길이 제한 확인 (message, title, content 등)
   - 최대 길이 없으면 추가 (message: 5000자, title: 200자, content: 2000자)
   - SQL injection 방지: JPA 파라미터 바인딩 사용 확인

B. JWT 보안
   - 토큰 만료 시간이 적절한지 확인 (24시간 이하 권장)
   - Authorization 헤더 파싱 시 Bearer 접두사 검증
   - 잘못된 형식의 토큰 → 401 반환 확인

C. API 응답 보안
   - 에러 메시지에 서버 내부 정보(스택 트레이스) 노출 안 되는지 확인
   - 존재하지 않는 사용자 조회 시 정보 유출 방지 (타이밍 공격 등)

D. CORS 설정 확인
   - 불필요하게 넓은 origin 허용 없는지

커밋 메시지: [R8-server] security: 보안 하드닝
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:27:04 → ios

```
[R8] 작업 지시 — 보안 하드닝

1. ~/Develop/aidy-ios/CLAUDE.md 읽기

작업:

A. Keychain 보안
   - JWT 토큰이 Keychain에 저장되는지 확인 (UserDefaults 금지)
   - Keychain 접근 시 적절한 accessibility level (kSecAttrAccessibleWhenUnlockedThisDeviceOnly)

B. 네트워크 보안
   - ATS (App Transport Security) 설정 확인
   - Certificate pinning은 추후 (현재는 ATS만)
   - API 요청 시 민감 데이터가 로그에 남지 않는지 확인

C. 데이터 보안
   - 스크린 캡처 시 민감 정보 마스킹 고려 (비밀번호 필드 등)
   - 백그라운드 시 앱 미리보기에서 내용 숨기기

커밋 메시지: [R8-ios] security: 보안 하드닝
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:27:55 → android

```
[R8] 작업 지시 — 보안 하드닝

1. ~/Develop/aidy-android/CLAUDE.md 읽기

작업:

A. 토큰 저장 보안
   - JWT가 EncryptedSharedPreferences에 저장되는지 확인
   - 일반 SharedPreferences 사용 금지
   - 토큰 만료 시 적절히 삭제

B. 네트워크 보안
   - network_security_config.xml에 cleartext 금지 확인
   - API 요청 시 민감 데이터 로그 제거 (OkHttp interceptor)

C. 데이터 보안
   - FLAG_SECURE 설정 고려 (스크린캡처 방지 - 비밀번호 화면만)
   - 백그라운드 최근 앱에서 내용 숨기기

커밋 메시지: [R8-android] security: 보안 하드닝
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:34:41 → server

```
[R9] 작업 지시 — 코드 정리 + 데드코드 제거

1. ~/Develop/aidy-server/CLAUDE.md 읽기

작업:

A. 미사용 코드 제거
   - import 정리 (사용하지 않는 import 제거)
   - 미사용 함수/변수 제거
   - 주석 처리된 코드 제거

B. 일관성 검토
   - DTO 클래스 네이밍 일관성 (Request/Response 접미사)
   - 서비스 메서드 네이밍 일관성
   - 에러 메시지 한국어 일관성

C. Kotlin 코드 스타일
   - data class 활용 (불필요한 class가 data class여야 할 곳)
   - null safety 개선 (!! 사용 최소화)
   - when/if-else 표현식 활용

커밋 메시지: [R9-server] refactor: 코드 정리
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:35:37 → ios

```
[R9] 작업 지시 — 코드 정리

1. ~/Develop/aidy-ios/CLAUDE.md 읽기

작업:

A. 미사용 코드 제거
   - import 정리
   - 미사용 함수/프로퍼티 제거
   - 주석 처리된 코드 제거

B. Swift 코드 스타일
   - SwiftLint 규칙 준수 확인
   - 불필요한 self 참조 제거
   - guard let 활용 최적화

C. TCA 패턴 일관성
   - Reducer 내 Action/State 정리
   - 미사용 Action case 제거
   - Effect 취소 ID 일관성

커밋 메시지: [R9-ios] refactor: 코드 정리
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:36:29 → android

```
[R9] 작업 지시 — 코드 정리

1. ~/Develop/aidy-android/CLAUDE.md 읽기

작업:

A. 미사용 코드 제거
   - import 정리
   - 미사용 함수/변수 제거
   - 주석 처리된 코드 제거

B. Kotlin 코드 스타일
   - 컴파일러 경고 0건 달성
   - 불필요한 nullable (?) 제거
   - sealed class/interface 활용 일관성

C. Compose 패턴 일관성
   - Modifier 파라미터 위치 일관성 (첫 번째 파라미터)
   - Preview 함수 정리
   - @Composable 함수 네이밍 (PascalCase)

커밋 메시지: [R9-android] refactor: 코드 정리
커밋 1건당 파일 10개 이하.
완료되면 idle 상태로 대기.
```

---

## 18:43:14 → server

```
[R10] 최종 검증 — 전체 테스트 실행 + 이상 보고

1. ~/Develop/aidy-server/CLAUDE.md 읽기
2. ./gradlew test 실행하여 전체 테스트 통과 확인
3. 테스트 결과 카운트 보고 (총 테스트 수, 실패 수)
4. 만약 실패하는 테스트가 있으면 수정

커밋 메시지: [R10-server] test: 최종 검증 (필요 시에만)
완료되면 idle 상태로 대기.
```

---

## 18:44:03 → ios

```
[R10] 최종 검증 — 빌드 + 테스트

1. ~/Develop/aidy-ios/CLAUDE.md 읽기
2. 프로젝트 빌드 실행 (tuist generate 후 xcodebuild 또는 swift build)
3. 테스트 실행 가능하면 실행
4. 빌드 에러 있으면 수정
5. 테스트 결과 카운트 보고

커밋 메시지: [R10-ios] fix: 최종 검증 수정 (필요 시에만)
완료되면 idle 상태로 대기.
```

---

## 18:44:52 → android

```
[R10] 최종 검증 — 빌드 + 테스트

1. ~/Develop/aidy-android/CLAUDE.md 읽기
2. ./gradlew assembleDebug 실행하여 빌드 확인
3. ./gradlew test 실행하여 테스트 확인
4. 빌드/테스트 에러 있으면 수정
5. 테스트 결과 카운트 보고

커밋 메시지: [R10-android] fix: 최종 검증 수정 (필요 시에만)
완료되면 idle 상태로 대기.
```

---

## 18:53:27 → server

```
/exit
```

---

## 18:54:14 → ios

```
/exit
```

---

## 18:55:03 → android

```
/exit
```
